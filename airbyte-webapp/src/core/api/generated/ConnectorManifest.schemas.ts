/* eslint-disable */
/**
 * Generated by orval 🍺
 * Do not edit manually. Run "pnpm run generate-client" instead.
 * Connector Manifest schema
 * OpenAPI spec version: 1.0.0
 */
export type DeclarativeComponentSchemaType =
  (typeof DeclarativeComponentSchemaType)[keyof typeof DeclarativeComponentSchemaType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeclarativeComponentSchemaType = {
  DeclarativeSource: "DeclarativeSource",
} as const;

export type DeclarativeComponentSchemaCheck = CheckStream | CheckDynamicStream;

export type DeclarativeComponentSchemaStreamsItem = ConditionalStreams | DeclarativeStream | StateDelegatingStream;

export type DeclarativeComponentSchemaDefinitions = { [key: string]: unknown };

/**
 * Maximum number of concurrent asynchronous jobs to run. This property is only relevant for sources/streams that support asynchronous job execution through the AsyncRetriever (e.g. a report-based stream that initiates a job, polls the job status, and then fetches the job results). This is often set by the API's maximum number of concurrent jobs on the account level. Refer to the API's documentation for this information.
 */
export type DeclarativeComponentSchemaMaxConcurrentAsyncJobCount = number | string;

/**
 * For internal Airbyte use only - DO NOT modify manually. Used by consumers of declarative manifests for storing related metadata.
 */
export type DeclarativeComponentSchemaMetadata = { [key: string]: unknown };

/**
 * An API source that extracts data according to its declarative components.
 */
export type DeclarativeComponentSchema =
  | (unknown & {
      type: DeclarativeComponentSchemaType;
      check: DeclarativeComponentSchemaCheck;
      streams?: DeclarativeComponentSchemaStreamsItem[];
      dynamic_streams?: DynamicDeclarativeStream[];
      /** The version of the Airbyte CDK used to build and test the source. */
      version: string;
      schemas?: Schemas;
      definitions?: DeclarativeComponentSchemaDefinitions;
      spec?: Spec;
      concurrency_level?: ConcurrencyLevel;
      api_budget?: HTTPAPIBudget;
      /** Maximum number of concurrent asynchronous jobs to run. This property is only relevant for sources/streams that support asynchronous job execution through the AsyncRetriever (e.g. a report-based stream that initiates a job, polls the job status, and then fetches the job results). This is often set by the API's maximum number of concurrent jobs on the account level. Refer to the API's documentation for this information. */
      max_concurrent_async_job_count?: DeclarativeComponentSchemaMaxConcurrentAsyncJobCount;
      /** For internal Airbyte use only - DO NOT modify manually. Used by consumers of declarative manifests for storing related metadata. */
      metadata?: DeclarativeComponentSchemaMetadata;
      /** A description of the connector. It will be presented on the Source documentation page. */
      description?: string;
    })
  | (unknown & {
      type: DeclarativeComponentSchemaType;
      check: DeclarativeComponentSchemaCheck;
      streams?: DeclarativeComponentSchemaStreamsItem[];
      dynamic_streams?: DynamicDeclarativeStream[];
      /** The version of the Airbyte CDK used to build and test the source. */
      version: string;
      schemas?: Schemas;
      definitions?: DeclarativeComponentSchemaDefinitions;
      spec?: Spec;
      concurrency_level?: ConcurrencyLevel;
      api_budget?: HTTPAPIBudget;
      /** Maximum number of concurrent asynchronous jobs to run. This property is only relevant for sources/streams that support asynchronous job execution through the AsyncRetriever (e.g. a report-based stream that initiates a job, polls the job status, and then fetches the job results). This is often set by the API's maximum number of concurrent jobs on the account level. Refer to the API's documentation for this information. */
      max_concurrent_async_job_count?: DeclarativeComponentSchemaMaxConcurrentAsyncJobCount;
      /** For internal Airbyte use only - DO NOT modify manually. Used by consumers of declarative manifests for storing related metadata. */
      metadata?: DeclarativeComponentSchemaMetadata;
      /** A description of the connector. It will be presented on the Source documentation page. */
      description?: string;
    });

export type AddedFieldDefinitionType = (typeof AddedFieldDefinitionType)[keyof typeof AddedFieldDefinitionType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AddedFieldDefinitionType = {
  AddedFieldDefinition: "AddedFieldDefinition",
} as const;

export type AddedFieldDefinitionParameters = { [key: string]: unknown };

/**
 * Defines the field to add on a record.
 */
export interface AddedFieldDefinition {
  type: AddedFieldDefinitionType;
  /** List of strings defining the path where to add the value on the record. */
  path: string[];
  /** Value of the new field. Use {{ record['existing_field'] }} syntax to refer to other fields in the record. */
  value: string;
  /** Type of the value. If not specified, the type will be inferred from the value. */
  value_type?: ValueType;
  $parameters?: AddedFieldDefinitionParameters;
}

export type AddFieldsType = (typeof AddFieldsType)[keyof typeof AddFieldsType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AddFieldsType = {
  AddFields: "AddFields",
} as const;

export type AddFieldsParameters = { [key: string]: unknown };

/**
 * Transformation which adds field to an output record. The path of the added field can be nested.
 */
export interface AddFields {
  type: AddFieldsType;
  /** List of transformations (path and corresponding value) that will be added to the record. */
  fields: AddedFieldDefinition[];
  /** Fields will be added if expression is evaluated to True. */
  condition?: string;
  $parameters?: AddFieldsParameters;
}

export type ApiKeyAuthenticatorType = (typeof ApiKeyAuthenticatorType)[keyof typeof ApiKeyAuthenticatorType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ApiKeyAuthenticatorType = {
  ApiKeyAuthenticator: "ApiKeyAuthenticator",
} as const;

export type ApiKeyAuthenticatorParameters = { [key: string]: unknown };

/**
 * Authenticator for requests authenticated with an API token injected as an HTTP request header.
 */
export interface ApiKeyAuthenticator {
  type: ApiKeyAuthenticatorType;
  /** The API key to inject in the request. Fill it in the user inputs. */
  api_token?: string;
  /** The name of the HTTP header that will be set to the API key. This setting is deprecated, use inject_into instead. Header and inject_into can not be defined at the same time. */
  header?: string;
  /** Configure how the API Key will be sent in requests to the source API. Either inject_into or header has to be defined. */
  inject_into?: RequestOption;
  $parameters?: ApiKeyAuthenticatorParameters;
}

/**
 * The type of auth to use
 */
export type AuthFlowAuthFlowType = (typeof AuthFlowAuthFlowType)[keyof typeof AuthFlowAuthFlowType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AuthFlowAuthFlowType = {
  oauth20: "oauth2.0",
  oauth10: "oauth1.0",
} as const;

/**
 * Additional and optional specification object to describe what an 'advanced' Auth flow would need to function.
  - A connector should be able to fully function with the configuration as described by the ConnectorSpecification in a 'basic' mode.
  - The 'advanced' mode provides easier UX for the user with UI improvements and automations. However, this requires further setup on the
  server side by instance or workspace admins beforehand. The trade-off is that the user does not have to provide as many technical
  inputs anymore and the auth process is faster and easier to complete.
 */
export interface AuthFlow {
  /** The type of auth to use */
  auth_flow_type?: AuthFlowAuthFlowType;
  /** JSON path to a field in the connectorSpecification that should exist for the advanced auth to be applicable. */
  predicate_key?: string[];
  /** Value of the predicate_key fields for the advanced auth to be applicable. */
  predicate_value?: string;
  oauth_config_specification?: OAuthConfigSpecification;
}

export type BasicHttpAuthenticatorType = (typeof BasicHttpAuthenticatorType)[keyof typeof BasicHttpAuthenticatorType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const BasicHttpAuthenticatorType = {
  BasicHttpAuthenticator: "BasicHttpAuthenticator",
} as const;

export type BasicHttpAuthenticatorParameters = { [key: string]: unknown };

/**
 * Authenticator for requests authenticated with the Basic HTTP authentication scheme, which encodes a username and an optional password in the Authorization request header.
 */
export interface BasicHttpAuthenticator {
  type: BasicHttpAuthenticatorType;
  /** The username that will be combined with the password, base64 encoded and used to make requests. Fill it in the user inputs. */
  username: string;
  /** The password that will be combined with the username, base64 encoded and used to make requests. Fill it in the user inputs. */
  password?: string;
  $parameters?: BasicHttpAuthenticatorParameters;
}

export type BearerAuthenticatorType = (typeof BearerAuthenticatorType)[keyof typeof BearerAuthenticatorType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const BearerAuthenticatorType = {
  BearerAuthenticator: "BearerAuthenticator",
} as const;

export type BearerAuthenticatorParameters = { [key: string]: unknown };

/**
 * Authenticator for requests authenticated with a bearer token injected as a request header of the form `Authorization: Bearer <token>`.
 */
export interface BearerAuthenticator {
  type: BearerAuthenticatorType;
  /** Token to inject as request header for authenticating with the API. */
  api_token: string;
  $parameters?: BearerAuthenticatorParameters;
}

export type SelectiveAuthenticatorType = (typeof SelectiveAuthenticatorType)[keyof typeof SelectiveAuthenticatorType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SelectiveAuthenticatorType = {
  SelectiveAuthenticator: "SelectiveAuthenticator",
} as const;

/**
 * Authenticators to select from.
 */
export type SelectiveAuthenticatorAuthenticators = {
  [key: string]:
    | ApiKeyAuthenticator
    | BasicHttpAuthenticator
    | BearerAuthenticator
    | OAuthAuthenticator
    | JwtAuthenticator
    | SessionTokenAuthenticator
    | LegacySessionTokenAuthenticator
    | CustomAuthenticator
    | NoAuth;
};

export type SelectiveAuthenticatorParameters = { [key: string]: unknown };

/**
 * Authenticator that selects concrete authenticator based on config property.
 */
export interface SelectiveAuthenticator {
  type: SelectiveAuthenticatorType;
  /** Path of the field in config with selected authenticator name */
  authenticator_selection_path: string[];
  /** Authenticators to select from. */
  authenticators: SelectiveAuthenticatorAuthenticators;
  $parameters?: SelectiveAuthenticatorParameters;
  [key: string]: unknown;
}

export type CheckStreamType = (typeof CheckStreamType)[keyof typeof CheckStreamType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CheckStreamType = {
  CheckStream: "CheckStream",
} as const;

/**
 * Defines the streams to try reading when running a check operation.
 */
export interface CheckStream {
  type: CheckStreamType;
  /** Names of the streams to try reading from when running a check operation. */
  stream_names?: string[];
  dynamic_streams_check_configs?: DynamicStreamCheckConfig[];
}

export type DynamicStreamCheckConfigType =
  (typeof DynamicStreamCheckConfigType)[keyof typeof DynamicStreamCheckConfigType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DynamicStreamCheckConfigType = {
  DynamicStreamCheckConfig: "DynamicStreamCheckConfig",
} as const;

export interface DynamicStreamCheckConfig {
  type: DynamicStreamCheckConfigType;
  /** The dynamic stream name. */
  dynamic_stream_name: string;
  /** The number of streams to attempt reading from during a check operation. If `stream_count` exceeds the total number of available streams, the minimum of the two values will be used. */
  stream_count?: number;
}

export type CheckDynamicStreamType = (typeof CheckDynamicStreamType)[keyof typeof CheckDynamicStreamType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CheckDynamicStreamType = {
  CheckDynamicStream: "CheckDynamicStream",
} as const;

/**
 * (This component is experimental. Use at your own risk.) Defines the dynamic streams to try reading when running a check operation.
 */
export interface CheckDynamicStream {
  type: CheckDynamicStreamType;
  /** Numbers of the streams to try reading from when running a check operation. */
  stream_count: number;
  /** Enables stream check availability. This field is automatically set by the CDK. */
  use_check_availability?: boolean;
}

export type CompositeErrorHandlerType = (typeof CompositeErrorHandlerType)[keyof typeof CompositeErrorHandlerType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CompositeErrorHandlerType = {
  CompositeErrorHandler: "CompositeErrorHandler",
} as const;

export type CompositeErrorHandlerErrorHandlersItem = CompositeErrorHandler | DefaultErrorHandler | CustomErrorHandler;

export type CompositeErrorHandlerParameters = { [key: string]: unknown };

/**
 * Error handler that sequentially iterates over a list of error handlers.
 */
export interface CompositeErrorHandler {
  type: CompositeErrorHandlerType;
  /** List of error handlers to iterate on to determine how to handle a failed response. */
  error_handlers: CompositeErrorHandlerErrorHandlersItem[];
  $parameters?: CompositeErrorHandlerParameters;
}

export type ConcurrencyLevelType = (typeof ConcurrencyLevelType)[keyof typeof ConcurrencyLevelType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ConcurrencyLevelType = {
  ConcurrencyLevel: "ConcurrencyLevel",
} as const;

/**
 * The amount of concurrency that will applied during a sync. This value can be hardcoded or user-defined in the config if different users have varying volume thresholds in the target API.
 */
export type ConcurrencyLevelDefaultConcurrency = number | string;

export type ConcurrencyLevelParameters = { [key: string]: unknown };

/**
 * Defines the amount of parallelization for the streams that are being synced. The factor of parallelization is how many partitions or streams are synced at the same time. For example, with a concurrency_level of 10, ten streams or partitions of data will processed at the same time. Note that a value of 1 could create deadlock if a stream has a very high number of partitions.
 */
export interface ConcurrencyLevel {
  type?: ConcurrencyLevelType;
  /** The amount of concurrency that will applied during a sync. This value can be hardcoded or user-defined in the config if different users have varying volume thresholds in the target API. */
  default_concurrency: ConcurrencyLevelDefaultConcurrency;
  /** The maximum level of concurrency that will be used during a sync. This becomes a required field when the default_concurrency derives from the config, because it serves as a safeguard against a user-defined threshold that is too high. */
  max_concurrency?: number;
  $parameters?: ConcurrencyLevelParameters;
}

export type ConditionalStreamsType = (typeof ConditionalStreamsType)[keyof typeof ConditionalStreamsType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ConditionalStreamsType = {
  ConditionalStreams: "ConditionalStreams",
} as const;

export type ConditionalStreamsParameters = { [key: string]: unknown };

/**
 * Streams that are only available while performing a connector operation when the condition is met.
 */
export interface ConditionalStreams {
  type: ConditionalStreamsType;
  /** Condition that will be evaluated to determine if a set of streams should be available. */
  condition: string;
  /** Streams that will be used during an operation based on the condition. */
  streams: DeclarativeStream[];
  $parameters?: ConditionalStreamsParameters;
}

export type ConstantBackoffStrategyType =
  (typeof ConstantBackoffStrategyType)[keyof typeof ConstantBackoffStrategyType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ConstantBackoffStrategyType = {
  ConstantBackoffStrategy: "ConstantBackoffStrategy",
} as const;

/**
 * Backoff time in seconds.
 */
export type ConstantBackoffStrategyBackoffTimeInSeconds = number | string;

export type ConstantBackoffStrategyParameters = { [key: string]: unknown };

/**
 * Backoff strategy with a constant backoff interval.
 */
export interface ConstantBackoffStrategy {
  type: ConstantBackoffStrategyType;
  /** Backoff time in seconds. */
  backoff_time_in_seconds: ConstantBackoffStrategyBackoffTimeInSeconds;
  $parameters?: ConstantBackoffStrategyParameters;
}

export type CursorPaginationType = (typeof CursorPaginationType)[keyof typeof CursorPaginationType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CursorPaginationType = {
  CursorPagination: "CursorPagination",
} as const;

/**
 * The number of records to include in each pages.
 */
export type CursorPaginationPageSize = number | string;

export type CursorPaginationParameters = { [key: string]: unknown };

/**
 * Pagination strategy that evaluates an interpolated string to define the next page to fetch.
 */
export interface CursorPagination {
  type: CursorPaginationType;
  /** Value of the cursor defining the next page to fetch. */
  cursor_value: string;
  /** The number of records to include in each pages. */
  page_size?: CursorPaginationPageSize;
  /** Template string evaluating when to stop paginating. */
  stop_condition?: string;
  $parameters?: CursorPaginationParameters;
}

export type CustomAuthenticatorType = (typeof CustomAuthenticatorType)[keyof typeof CustomAuthenticatorType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomAuthenticatorType = {
  CustomAuthenticator: "CustomAuthenticator",
} as const;

export type CustomAuthenticatorParameters = { [key: string]: unknown };

/**
 * Authenticator component whose behavior is derived from a custom code implementation of the connector.
 */
export interface CustomAuthenticator {
  type: CustomAuthenticatorType;
  /** Fully-qualified name of the class that will be implementing the custom authentication strategy. Has to be a sub class of DeclarativeAuthenticator. The format is `source_<name>.<package>.<class_name>`. */
  class_name: string;
  $parameters?: CustomAuthenticatorParameters;
  [key: string]: unknown;
}

export type CustomBackoffStrategyType = (typeof CustomBackoffStrategyType)[keyof typeof CustomBackoffStrategyType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomBackoffStrategyType = {
  CustomBackoffStrategy: "CustomBackoffStrategy",
} as const;

export type CustomBackoffStrategyParameters = { [key: string]: unknown };

/**
 * Backoff strategy component whose behavior is derived from a custom code implementation of the connector.
 */
export interface CustomBackoffStrategy {
  type: CustomBackoffStrategyType;
  /** Fully-qualified name of the class that will be implementing the custom backoff strategy. The format is `source_<name>.<package>.<class_name>`. */
  class_name: string;
  $parameters?: CustomBackoffStrategyParameters;
  [key: string]: unknown;
}

export type CustomErrorHandlerType = (typeof CustomErrorHandlerType)[keyof typeof CustomErrorHandlerType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomErrorHandlerType = {
  CustomErrorHandler: "CustomErrorHandler",
} as const;

export type CustomErrorHandlerParameters = { [key: string]: unknown };

/**
 * Error handler component whose behavior is derived from a custom code implementation of the connector.
 */
export interface CustomErrorHandler {
  type: CustomErrorHandlerType;
  /** Fully-qualified name of the class that will be implementing the custom error handler. The format is `source_<name>.<package>.<class_name>`. */
  class_name: string;
  $parameters?: CustomErrorHandlerParameters;
  [key: string]: unknown;
}

export type CustomPaginationStrategyType =
  (typeof CustomPaginationStrategyType)[keyof typeof CustomPaginationStrategyType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomPaginationStrategyType = {
  CustomPaginationStrategy: "CustomPaginationStrategy",
} as const;

export type CustomPaginationStrategyParameters = { [key: string]: unknown };

/**
 * Pagination strategy component whose behavior is derived from a custom code implementation of the connector.
 */
export interface CustomPaginationStrategy {
  type: CustomPaginationStrategyType;
  /** Fully-qualified name of the class that will be implementing the custom pagination strategy. The format is `source_<name>.<package>.<class_name>`. */
  class_name: string;
  $parameters?: CustomPaginationStrategyParameters;
  [key: string]: unknown;
}

export type CustomRecordExtractorType = (typeof CustomRecordExtractorType)[keyof typeof CustomRecordExtractorType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomRecordExtractorType = {
  CustomRecordExtractor: "CustomRecordExtractor",
} as const;

export type CustomRecordExtractorParameters = { [key: string]: unknown };

/**
 * Record extractor component whose behavior is derived from a custom code implementation of the connector.
 */
export interface CustomRecordExtractor {
  type: CustomRecordExtractorType;
  /** Fully-qualified name of the class that will be implementing the custom record extraction strategy. The format is `source_<name>.<package>.<class_name>`. */
  class_name: string;
  $parameters?: CustomRecordExtractorParameters;
  [key: string]: unknown;
}

export type CustomRecordFilterType = (typeof CustomRecordFilterType)[keyof typeof CustomRecordFilterType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomRecordFilterType = {
  CustomRecordFilter: "CustomRecordFilter",
} as const;

export type CustomRecordFilterParameters = { [key: string]: unknown };

/**
 * Record filter component whose behavior is derived from a custom code implementation of the connector.
 */
export interface CustomRecordFilter {
  type: CustomRecordFilterType;
  /** Fully-qualified name of the class that will be implementing the custom record filter strategy. The format is `source_<name>.<package>.<class_name>`. */
  class_name: string;
  $parameters?: CustomRecordFilterParameters;
  [key: string]: unknown;
}

export type CustomRequesterType = (typeof CustomRequesterType)[keyof typeof CustomRequesterType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomRequesterType = {
  CustomRequester: "CustomRequester",
} as const;

export type CustomRequesterParameters = { [key: string]: unknown };

/**
 * Requester component whose behavior is derived from a custom code implementation of the connector.
 */
export interface CustomRequester {
  type: CustomRequesterType;
  /** Fully-qualified name of the class that will be implementing the custom requester strategy. The format is `source_<name>.<package>.<class_name>`. */
  class_name: string;
  $parameters?: CustomRequesterParameters;
  [key: string]: unknown;
}

export type CustomRetrieverType = (typeof CustomRetrieverType)[keyof typeof CustomRetrieverType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomRetrieverType = {
  CustomRetriever: "CustomRetriever",
} as const;

export type CustomRetrieverParameters = { [key: string]: unknown };

/**
 * Retriever component whose behavior is derived from a custom code implementation of the connector.
 */
export interface CustomRetriever {
  type: CustomRetrieverType;
  /** Fully-qualified name of the class that will be implementing the custom retriever strategy. The format is `source_<name>.<package>.<class_name>`. */
  class_name: string;
  $parameters?: CustomRetrieverParameters;
  [key: string]: unknown;
}

export type CustomPartitionRouterType = (typeof CustomPartitionRouterType)[keyof typeof CustomPartitionRouterType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomPartitionRouterType = {
  CustomPartitionRouter: "CustomPartitionRouter",
} as const;

export type CustomPartitionRouterParameters = { [key: string]: unknown };

/**
 * Partition router component whose behavior is derived from a custom code implementation of the connector.
 */
export interface CustomPartitionRouter {
  type: CustomPartitionRouterType;
  /** Fully-qualified name of the class that will be implementing the custom partition router. The format is `source_<name>.<package>.<class_name>`. */
  class_name: string;
  $parameters?: CustomPartitionRouterParameters;
  [key: string]: unknown;
}

export type CustomSchemaLoaderType = (typeof CustomSchemaLoaderType)[keyof typeof CustomSchemaLoaderType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomSchemaLoaderType = {
  CustomSchemaLoader: "CustomSchemaLoader",
} as const;

export type CustomSchemaLoaderParameters = { [key: string]: unknown };

/**
 * Schema Loader component whose behavior is derived from a custom code implementation of the connector.
 */
export interface CustomSchemaLoader {
  type: CustomSchemaLoaderType;
  /** Fully-qualified name of the class that will be implementing the custom schema loader. The format is `source_<name>.<package>.<class_name>`. */
  class_name: string;
  $parameters?: CustomSchemaLoaderParameters;
  [key: string]: unknown;
}

export type CustomSchemaNormalizationType =
  (typeof CustomSchemaNormalizationType)[keyof typeof CustomSchemaNormalizationType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomSchemaNormalizationType = {
  CustomSchemaNormalization: "CustomSchemaNormalization",
} as const;

export type CustomSchemaNormalizationParameters = { [key: string]: unknown };

/**
 * Schema normalization component whose behavior is derived from a custom code implementation of the connector.
 */
export interface CustomSchemaNormalization {
  type: CustomSchemaNormalizationType;
  /** Fully-qualified name of the class that will be implementing the custom normalization. The format is `source_<name>.<package>.<class_name>`. */
  class_name: string;
  $parameters?: CustomSchemaNormalizationParameters;
  [key: string]: unknown;
}

export type CustomStateMigrationType = (typeof CustomStateMigrationType)[keyof typeof CustomStateMigrationType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomStateMigrationType = {
  CustomStateMigration: "CustomStateMigration",
} as const;

export type CustomStateMigrationParameters = { [key: string]: unknown };

/**
 * Apply a custom transformation on the input state.
 */
export interface CustomStateMigration {
  type: CustomStateMigrationType;
  /** Fully-qualified name of the class that will be implementing the custom state migration. The format is `source_<name>.<package>.<class_name>`. */
  class_name: string;
  $parameters?: CustomStateMigrationParameters;
  [key: string]: unknown;
}

export type CustomTransformationType = (typeof CustomTransformationType)[keyof typeof CustomTransformationType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomTransformationType = {
  CustomTransformation: "CustomTransformation",
} as const;

export type CustomTransformationParameters = { [key: string]: unknown };

/**
 * Transformation component whose behavior is derived from a custom code implementation of the connector.
 */
export interface CustomTransformation {
  type: CustomTransformationType;
  /** Fully-qualified name of the class that will be implementing the custom transformation. The format is `source_<name>.<package>.<class_name>`. */
  class_name: string;
  $parameters?: CustomTransformationParameters;
  [key: string]: unknown;
}

export type LegacyToPerPartitionStateMigrationType =
  (typeof LegacyToPerPartitionStateMigrationType)[keyof typeof LegacyToPerPartitionStateMigrationType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LegacyToPerPartitionStateMigrationType = {
  LegacyToPerPartitionStateMigration: "LegacyToPerPartitionStateMigration",
} as const;

/**
 * Transforms the input state for per-partitioned streams from the legacy format to the low-code format. The cursor field and partition ID fields are automatically extracted from the stream's DatetimebasedCursor and SubstreamPartitionRouter.
Example input state: { "13506132": { "last_changed": "2022-12-27T08:34:39+00:00" } Example output state: { "partition": {"id": "13506132"}, "cursor": {"last_changed": "2022-12-27T08:34:39+00:00"} } 
 */
export interface LegacyToPerPartitionStateMigration {
  type?: LegacyToPerPartitionStateMigrationType;
  [key: string]: unknown;
}

export type IncrementingCountCursorType =
  (typeof IncrementingCountCursorType)[keyof typeof IncrementingCountCursorType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const IncrementingCountCursorType = {
  IncrementingCountCursor: "IncrementingCountCursor",
} as const;

/**
 * The value that determines the earliest record that should be synced.
 */
export type IncrementingCountCursorStartValue = string | number;

export type IncrementingCountCursorParameters = { [key: string]: unknown };

/**
 * Cursor that allows for incremental sync according to a continuously increasing integer.
 */
export interface IncrementingCountCursor {
  type: IncrementingCountCursorType;
  /** The location of the value on a record that will be used as a bookmark during sync. To ensure no data loss, the API must return records in ascending order based on the cursor field. Nested fields are not supported, so the field must be at the top level of the record. You can use a combination of Add Field and Remove Field transformations to move the nested field to the top. */
  cursor_field: string;
  /** Whether the cursor allows users to override the default cursor_field when configuring their connection. The user defined cursor field will be specified from within the configured catalog. */
  allow_catalog_defined_cursor_field?: boolean;
  /** The value that determines the earliest record that should be synced. */
  start_value?: IncrementingCountCursorStartValue;
  /** Optionally configures how the start value will be sent in requests to the source API. */
  start_value_option?: RequestOption;
  $parameters?: IncrementingCountCursorParameters;
}

export type DatetimeBasedCursorType = (typeof DatetimeBasedCursorType)[keyof typeof DatetimeBasedCursorType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DatetimeBasedCursorType = {
  DatetimeBasedCursor: "DatetimeBasedCursor",
} as const;

export type DatetimeBasedCursorClampingTargetDetails = { [key: string]: unknown };

/**
 * This option is used to adjust the upper and lower boundaries of each datetime window to beginning and end of the provided target period (day, week, month)
 */
export type DatetimeBasedCursorClamping = {
  /** The period of time that datetime windows will be clamped by */
  target: string;
  target_details?: DatetimeBasedCursorClampingTargetDetails;
};

/**
 * The datetime that determines the earliest record that should be synced.
 */
export type DatetimeBasedCursorStartDatetime = MinMaxDatetime | string;

/**
 * The datetime that determines the last record that should be synced. If not provided, `{{ now_utc() }}` will be used.
 */
export type DatetimeBasedCursorEndDatetime = MinMaxDatetime | string;

export type DatetimeBasedCursorParameters = { [key: string]: unknown };

/**
 * Cursor to provide incremental capabilities over datetime.
 */
export interface DatetimeBasedCursor {
  type: DatetimeBasedCursorType;
  /** This option is used to adjust the upper and lower boundaries of each datetime window to beginning and end of the provided target period (day, week, month) */
  clamping?: DatetimeBasedCursorClamping;
  /** The location of the value on a record that will be used as a bookmark during sync. To ensure no data loss, the API must return records in ascending order based on the cursor field. Nested fields are not supported, so the field must be at the top level of the record. You can use a combination of Add Field and Remove Field transformations to move the nested field to the top. */
  cursor_field: string;
  /** Whether the cursor allows users to override the default cursor_field when configuring their connection. The user defined cursor field will be specified from within the configured catalog. */
  allow_catalog_defined_cursor_field?: boolean;
  /** The possible formats for the cursor field, in order of preference. The first format that matches the cursor field value will be used to parse it. If not provided, the Outgoing Datetime Format will be used.
Use placeholders starting with "%" to describe the format the API is using. The following placeholders are available:
  * **%s**: Epoch unix timestamp - `1686218963`
  * **%s_as_float**: Epoch unix timestamp in seconds as float with microsecond precision - `1686218963.123456`
  * **%ms**: Epoch unix timestamp - `1686218963123`
  * **%a**: Weekday (abbreviated) - `Sun`
  * **%A**: Weekday (full) - `Sunday`
  * **%w**: Weekday (decimal) - `0` (Sunday), `6` (Saturday)
  * **%d**: Day of the month (zero-padded) - `01`, `02`, ..., `31`
  * **%b**: Month (abbreviated) - `Jan`
  * **%B**: Month (full) - `January`
  * **%m**: Month (zero-padded) - `01`, `02`, ..., `12`
  * **%y**: Year (without century, zero-padded) - `00`, `01`, ..., `99`
  * **%Y**: Year (with century) - `0001`, `0002`, ..., `9999`
  * **%H**: Hour (24-hour, zero-padded) - `00`, `01`, ..., `23`
  * **%I**: Hour (12-hour, zero-padded) - `01`, `02`, ..., `12`
  * **%p**: AM/PM indicator
  * **%M**: Minute (zero-padded) - `00`, `01`, ..., `59`
  * **%S**: Second (zero-padded) - `00`, `01`, ..., `59`
  * **%f**: Microsecond (zero-padded to 6 digits) - `000000`, `000001`, ..., `999999`
  * **%_ms**: Millisecond (zero-padded to 3 digits) - `000`, `001`, ..., `999`
  * **%z**: UTC offset - `(empty)`, `+0000`, `-04:00`
  * **%Z**: Time zone name - `(empty)`, `UTC`, `GMT`
  * **%j**: Day of the year (zero-padded) - `001`, `002`, ..., `366`
  * **%U**: Week number of the year (Sunday as first day) - `00`, `01`, ..., `53`
  * **%W**: Week number of the year (Monday as first day) - `00`, `01`, ..., `53`
  * **%c**: Date and time representation - `Tue Aug 16 21:30:00 1988`
  * **%x**: Date representation - `08/16/1988`
  * **%X**: Time representation - `21:30:00`
  * **%%**: Literal '%' character

  Some placeholders depend on the locale of the underlying system - in most cases this locale is configured as en/US. For more information see the [Python documentation](https://docs.python.org/3/library/datetime.html#strftime-and-strptime-format-codes).
 */
  cursor_datetime_formats?: string[];
  /** The datetime that determines the earliest record that should be synced. */
  start_datetime: DatetimeBasedCursorStartDatetime;
  /** Optionally configures how the start datetime will be sent in requests to the source API. */
  start_time_option?: RequestOption;
  /** The datetime that determines the last record that should be synced. If not provided, `{{ now_utc() }}` will be used. */
  end_datetime?: DatetimeBasedCursorEndDatetime;
  /** Optionally configures how the end datetime will be sent in requests to the source API. */
  end_time_option?: RequestOption;
  /** The datetime format used to format the datetime values that are sent in outgoing requests to the API. Use placeholders starting with "%" to describe the format the API is using. The following placeholders are available:
  * **%s**: Epoch unix timestamp - `1686218963`
  * **%s_as_float**: Epoch unix timestamp in seconds as float with microsecond precision - `1686218963.123456`
  * **%ms**: Epoch unix timestamp (milliseconds) - `1686218963123`
  * **%a**: Weekday (abbreviated) - `Sun`
  * **%A**: Weekday (full) - `Sunday`
  * **%w**: Weekday (decimal) - `0` (Sunday), `6` (Saturday)
  * **%d**: Day of the month (zero-padded) - `01`, `02`, ..., `31`
  * **%b**: Month (abbreviated) - `Jan`
  * **%B**: Month (full) - `January`
  * **%m**: Month (zero-padded) - `01`, `02`, ..., `12`
  * **%y**: Year (without century, zero-padded) - `00`, `01`, ..., `99`
  * **%Y**: Year (with century) - `0001`, `0002`, ..., `9999`
  * **%H**: Hour (24-hour, zero-padded) - `00`, `01`, ..., `23`
  * **%I**: Hour (12-hour, zero-padded) - `01`, `02`, ..., `12`
  * **%p**: AM/PM indicator
  * **%M**: Minute (zero-padded) - `00`, `01`, ..., `59`
  * **%S**: Second (zero-padded) - `00`, `01`, ..., `59`
  * **%f**: Microsecond (zero-padded to 6 digits) - `000000`
  * **%_ms**: Millisecond (zero-padded to 3 digits) - `000`
  * **%z**: UTC offset - `(empty)`, `+0000`, `-04:00`
  * **%Z**: Time zone name - `(empty)`, `UTC`, `GMT`
  * **%j**: Day of the year (zero-padded) - `001`, `002`, ..., `366`
  * **%U**: Week number of the year (starting Sunday) - `00`, ..., `53`
  * **%W**: Week number of the year (starting Monday) - `00`, ..., `53`
  * **%c**: Date and time - `Tue Aug 16 21:30:00 1988`
  * **%x**: Date standard format - `08/16/1988`
  * **%X**: Time standard format - `21:30:00`
  * **%%**: Literal '%' character

  Some placeholders depend on the locale of the underlying system - in most cases this locale is configured as en/US. For more information see the [Python documentation](https://docs.python.org/3/library/datetime.html#strftime-and-strptime-format-codes).
 */
  datetime_format: string;
  /** Smallest increment the datetime_format has (ISO 8601 duration) that is used to ensure the start of a slice does not overlap with the end of the previous one, e.g. for %Y-%m-%d the granularity should
be P1D, for %Y-%m-%dT%H:%M:%SZ the granularity should be PT1S. Given this field is provided, `step` needs to be provided as well.
  * **PT0.000001S**: 1 microsecond
  * **PT0.001S**: 1 millisecond
  * **PT1S**: 1 second
  * **PT1M**: 1 minute
  * **PT1H**: 1 hour
  * **P1D**: 1 day
 */
  cursor_granularity?: string;
  /** A data feed API is an API that does not allow filtering and paginates the content from the most recent to the least recent. Given this, the CDK needs to know when to stop paginating and this field will generate a stop condition for pagination. */
  is_data_feed?: boolean;
  /** Set to True if the target API endpoint does not take cursor values to filter records and returns all records anyway. This will cause the connector to filter out records locally, and only emit new records from the last sync, hence incremental. This means that all records would be read from the API, but only new records will be emitted to the destination. */
  is_client_side_incremental?: boolean;
  /** Set to True if the target API does not accept queries where the start time equal the end time. This will cause those requests to be skipped. */
  is_compare_strictly?: boolean;
  /** Setting to True causes the connector to store the cursor as one value, instead of per-partition. This setting optimizes performance when the parent stream has thousands of partitions. Notably, the substream state is updated only at the end of the sync, which helps prevent data loss in case of a sync failure. See more info in the [docs](https://docs.airbyte.com/connector-development/config-based/understanding-the-yaml-file/incremental-syncs). */
  global_substream_cursor?: boolean;
  /** Time interval (ISO8601 duration) before the start_datetime to read data for, e.g. P1M for looking back one month.
   * **PT1H**: 1 hour
   * **P1D**: 1 day
   * **P1W**: 1 week
   * **P1M**: 1 month
   * **P1Y**: 1 year
   */
  lookback_window?: string;
  /** Name of the partition start time field. */
  partition_field_end?: string;
  /** Name of the partition end time field. */
  partition_field_start?: string;
  /** The size of the time window (ISO8601 duration). Given this field is provided, `cursor_granularity` needs to be provided as well.
   * **PT1H**: 1 hour
   * **P1D**: 1 day
   * **P1W**: 1 week
   * **P1M**: 1 month
   * **P1Y**: 1 year
   */
  step?: string;
  $parameters?: DatetimeBasedCursorParameters;
}

export type JwtAuthenticatorType = (typeof JwtAuthenticatorType)[keyof typeof JwtAuthenticatorType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const JwtAuthenticatorType = {
  JwtAuthenticator: "JwtAuthenticator",
} as const;

/**
 * Algorithm used to sign the JSON web token.
 */
export type JwtAuthenticatorAlgorithm = (typeof JwtAuthenticatorAlgorithm)[keyof typeof JwtAuthenticatorAlgorithm];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const JwtAuthenticatorAlgorithm = {
  HS256: "HS256",
  HS384: "HS384",
  HS512: "HS512",
  ES256: "ES256",
  ES256K: "ES256K",
  ES384: "ES384",
  ES512: "ES512",
  RS256: "RS256",
  RS384: "RS384",
  RS512: "RS512",
  PS256: "PS256",
  PS384: "PS384",
  PS512: "PS512",
  EdDSA: "EdDSA",
} as const;

/**
 * JWT headers used when signing JSON web token.
 */
export type JwtAuthenticatorJwtHeaders = {
  /** Private key ID for user account. */
  kid?: string;
  /** The media type of the complete JWT. */
  typ?: string;
  /** Content type of JWT header. */
  cty?: string;
};

/**
 * Additional headers to be included with the JWT headers object.
 */
export type JwtAuthenticatorAdditionalJwtHeaders = { [key: string]: unknown };

/**
 * JWT Payload used when signing JSON web token.
 */
export type JwtAuthenticatorJwtPayload = {
  /** The user/principal that issued the JWT. Commonly a value unique to the user. */
  iss?: string;
  /** The subject of the JWT. Commonly defined by the API. */
  sub?: string;
  /** The recipient that the JWT is intended for. Commonly defined by the API. */
  aud?: string;
};

/**
 * Additional properties to be added to the JWT payload.
 */
export type JwtAuthenticatorAdditionalJwtPayload = { [key: string]: unknown };

export type JwtAuthenticatorParameters = { [key: string]: unknown };

/**
 * Authenticator for requests using JWT authentication flow.
 */
export interface JwtAuthenticator {
  type: JwtAuthenticatorType;
  /** Secret used to sign the JSON web token. */
  secret_key: string;
  /** When set to true, the secret key will be base64 encoded prior to being encoded as part of the JWT. Only set to "true" when required by the API. */
  base64_encode_secret_key?: boolean;
  /** Algorithm used to sign the JSON web token. */
  algorithm: JwtAuthenticatorAlgorithm;
  /** The amount of time in seconds a JWT token can be valid after being issued. */
  token_duration?: number;
  /** The prefix to be used within the Authentication header. */
  header_prefix?: string;
  /** JWT headers used when signing JSON web token. */
  jwt_headers?: JwtAuthenticatorJwtHeaders;
  /** Additional headers to be included with the JWT headers object. */
  additional_jwt_headers?: JwtAuthenticatorAdditionalJwtHeaders;
  /** JWT Payload used when signing JSON web token. */
  jwt_payload?: JwtAuthenticatorJwtPayload;
  /** Additional properties to be added to the JWT payload. */
  additional_jwt_payload?: JwtAuthenticatorAdditionalJwtPayload;
  /** A passphrase/password used to encrypt the private key. Only provide a passphrase if required by the API for JWT authentication. The API will typically provide the passphrase when generating the public/private key pair. */
  passphrase?: string;
  /** A request option describing where the signed JWT token that is generated should be injected into the outbound API request. */
  request_option?: RequestOption;
  $parameters?: JwtAuthenticatorParameters;
}

export type OAuthAuthenticatorType = (typeof OAuthAuthenticatorType)[keyof typeof OAuthAuthenticatorType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OAuthAuthenticatorType = {
  OAuthAuthenticator: "OAuthAuthenticator",
} as const;

/**
 * Body of the request sent to get a new access token.
 */
export type OAuthAuthenticatorRefreshRequestBody = { [key: string]: unknown };

/**
 * Headers of the request sent to get a new access token.
 */
export type OAuthAuthenticatorRefreshRequestHeaders = { [key: string]: unknown };

/**
 * When the refresh token updater is defined, new refresh tokens, access tokens and the access token expiry date are written back from the authentication response to the config object. This is important if the refresh token can only used once.
 */
export type OAuthAuthenticatorRefreshTokenUpdater = {
  /** The name of the property which contains the updated refresh token in the response from the token refresh endpoint. */
  refresh_token_name?: string;
  /** Config path to the access token. Make sure the field actually exists in the config. */
  access_token_config_path?: string[];
  /** Config path to the access token. Make sure the field actually exists in the config. */
  refresh_token_config_path?: string[];
  /** Config path to the expiry date. Make sure actually exists in the config. */
  token_expiry_date_config_path?: string[];
  /** Status Codes to Identify refresh token error in response (Refresh Token Error Key and Refresh Token Error Values should be also specified). Responses with one of the error status code and containing an error value will be flagged as a config error */
  refresh_token_error_status_codes?: number[];
  /** Key to Identify refresh token error in response (Refresh Token Error Status Codes and Refresh Token Error Values should be also specified). */
  refresh_token_error_key?: string;
  /** List of values to check for exception during token refresh process. Used to check if the error found in the response matches the key from the Refresh Token Error Key field (e.g. response={"error": "invalid_grant"}). Only responses with one of the error status code and containing an error value will be flagged as a config error */
  refresh_token_error_values?: string[];
};

export type OAuthAuthenticatorParameters = { [key: string]: unknown };

/**
 * Authenticator for requests using OAuth 2.0 authorization flow.
 */
export interface OAuthAuthenticator {
  type: OAuthAuthenticatorType;
  /** The name of the property to use to refresh the `access_token`. */
  client_id_name?: string;
  /** The OAuth client ID. Fill it in the user inputs. */
  client_id?: string;
  /** The name of the property to use to refresh the `access_token`. */
  client_secret_name?: string;
  /** The OAuth client secret. Fill it in the user inputs. */
  client_secret?: string;
  /** The name of the property to use to refresh the `access_token`. */
  refresh_token_name?: string;
  /** Credential artifact used to get a new access token. */
  refresh_token?: string;
  /** The full URL to call to obtain a new access token. */
  token_refresh_endpoint?: string;
  /** The name of the property which contains the access token in the response from the token refresh endpoint. */
  access_token_name?: string;
  /** The value of the access_token to bypass the token refreshing using `refresh_token`. */
  access_token_value?: string;
  /** The name of the property which contains the expiry date in the response from the token refresh endpoint. */
  expires_in_name?: string;
  /** The name of the property to use to refresh the `access_token`. */
  grant_type_name?: string;
  /** Specifies the OAuth2 grant type. If set to refresh_token, the refresh_token needs to be provided as well. For client_credentials, only client id and secret are required. Other grant types are not officially supported. */
  grant_type?: string;
  /** Body of the request sent to get a new access token. */
  refresh_request_body?: OAuthAuthenticatorRefreshRequestBody;
  /** Headers of the request sent to get a new access token. */
  refresh_request_headers?: OAuthAuthenticatorRefreshRequestHeaders;
  /** List of scopes that should be granted to the access token. */
  scopes?: string[];
  /** The access token expiry date. */
  token_expiry_date?: string;
  /** The format of the time to expiration datetime. Provide it if the time is returned as a date-time string instead of seconds. */
  token_expiry_date_format?: string;
  /** Status Codes to Identify refresh token error in response (Refresh Token Error Key and Refresh Token Error Values should be also specified). Responses with one of the error status code and containing an error value will be flagged as a config error */
  refresh_token_error_status_codes?: number[];
  /** Key to Identify refresh token error in response (Refresh Token Error Status Codes and Refresh Token Error Values should be also specified). */
  refresh_token_error_key?: string;
  /** List of values to check for exception during token refresh process. Used to check if the error found in the response matches the key from the Refresh Token Error Key field (e.g. response={"error": "invalid_grant"}). Only responses with one of the error status code and containing an error value will be flagged as a config error */
  refresh_token_error_values?: string[];
  /** When the refresh token updater is defined, new refresh tokens, access tokens and the access token expiry date are written back from the authentication response to the config object. This is important if the refresh token can only used once. */
  refresh_token_updater?: OAuthAuthenticatorRefreshTokenUpdater;
  /** The authenticator being used to authenticate the client authenticator. */
  profile_assertion?: JwtAuthenticator;
  /** Enable using profile assertion as a flow for OAuth authorization. */
  use_profile_assertion?: boolean;
  $parameters?: OAuthAuthenticatorParameters;
}

export type DeclarativeStreamType = (typeof DeclarativeStreamType)[keyof typeof DeclarativeStreamType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeclarativeStreamType = {
  DeclarativeStream: "DeclarativeStream",
} as const;

/**
 * Component used to coordinate how records are extracted across stream slices and request pages.
 */
export type DeclarativeStreamRetriever = SimpleRetriever | AsyncRetriever | CustomRetriever;

/**
 * Component used to fetch data incrementally based on a time field in the data.
 */
export type DeclarativeStreamIncrementalSync = DatetimeBasedCursor | IncrementingCountCursor;

export type DeclarativeStreamSchemaLoaderAnyOfItem =
  | InlineSchemaLoader
  | DynamicSchemaLoader
  | JsonFileSchemaLoader
  | CustomSchemaLoader;

/**
 * One or many schema loaders can be used to retrieve the schema for the current stream. When multiple schema loaders are defined, schema properties will be merged together. Schema loaders defined first taking precedence in the event of a conflict.
 */
export type DeclarativeStreamSchemaLoader =
  | InlineSchemaLoader
  | DynamicSchemaLoader
  | JsonFileSchemaLoader
  | DeclarativeStreamSchemaLoaderAnyOfItem[]
  | CustomSchemaLoader;

export type DeclarativeStreamTransformationsItem =
  | AddFields
  | RemoveFields
  | KeysToLower
  | KeysToSnakeCase
  | FlattenFields
  | DpathFlattenFields
  | KeysReplace
  | CustomTransformation;

export type DeclarativeStreamStateMigrationsItem = LegacyToPerPartitionStateMigration | CustomStateMigration;

export type DeclarativeStreamFileUploaderType =
  (typeof DeclarativeStreamFileUploaderType)[keyof typeof DeclarativeStreamFileUploaderType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DeclarativeStreamFileUploaderType = {
  FileUploader: "FileUploader",
} as const;

/**
 * Requester component that describes how to prepare HTTP requests to send to the source API.
 */
export type DeclarativeStreamFileUploaderRequester = HttpRequester | CustomRequester;

/**
 * Responsible for fetching the url where the file is located. This is applied on each records and not on the HTTP response
 */
export type DeclarativeStreamFileUploaderDownloadTargetExtractor = DpathExtractor | CustomRecordExtractor;

/**
 * Responsible for fetching the content of the file. If not defined, the assumption is that the whole response body is the file content
 */
export type DeclarativeStreamFileUploaderFileExtractor = DpathExtractor | CustomRecordExtractor;

export type DeclarativeStreamFileUploaderParameters = { [key: string]: unknown };

/**
 * (experimental) Describes how to fetch a file
 */
export type DeclarativeStreamFileUploader = {
  type: DeclarativeStreamFileUploaderType;
  /** Requester component that describes how to prepare HTTP requests to send to the source API. */
  requester: DeclarativeStreamFileUploaderRequester;
  /** Responsible for fetching the url where the file is located. This is applied on each records and not on the HTTP response */
  download_target_extractor: DeclarativeStreamFileUploaderDownloadTargetExtractor;
  /** Responsible for fetching the content of the file. If not defined, the assumption is that the whole response body is the file content */
  file_extractor?: DeclarativeStreamFileUploaderFileExtractor;
  /** Defines the name to store the file. Stream name is automatically added to the file path. File unique ID can be used to avoid overwriting files. Random UUID will be used if the extractor is not provided. */
  filename_extractor?: string;
  $parameters?: DeclarativeStreamFileUploaderParameters;
};

export type DeclarativeStreamParameters = { [key: string]: unknown };

/**
 * A stream whose behavior is described by a set of declarative low code components.
 */
export interface DeclarativeStream {
  type: DeclarativeStreamType;
  /** The stream name. */
  name?: string;
  /** Component used to coordinate how records are extracted across stream slices and request pages. */
  retriever: DeclarativeStreamRetriever;
  /** Component used to fetch data incrementally based on a time field in the data. */
  incremental_sync?: DeclarativeStreamIncrementalSync;
  primary_key?: PrimaryKey;
  /** One or many schema loaders can be used to retrieve the schema for the current stream. When multiple schema loaders are defined, schema properties will be merged together. Schema loaders defined first taking precedence in the event of a conflict. */
  schema_loader?: DeclarativeStreamSchemaLoader;
  /** A list of transformations to be applied to each output record. */
  transformations?: DeclarativeStreamTransformationsItem[];
  /** Array of state migrations to be applied on the input state */
  state_migrations?: DeclarativeStreamStateMigrationsItem[];
  /** (experimental) Describes how to fetch a file */
  file_uploader?: DeclarativeStreamFileUploader;
  $parameters?: DeclarativeStreamParameters;
  [key: string]: unknown;
}

export type HTTPAPIBudgetType = (typeof HTTPAPIBudgetType)[keyof typeof HTTPAPIBudgetType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const HTTPAPIBudgetType = {
  HTTPAPIBudget: "HTTPAPIBudget",
} as const;

export type HTTPAPIBudgetPoliciesItem =
  | FixedWindowCallRatePolicy
  | MovingWindowCallRatePolicy
  | UnlimitedCallRatePolicy;

/**
 * Defines how many requests can be made to the API in a given time frame. `HTTPAPIBudget` extracts the remaining call count and the reset time from HTTP response headers using the header names provided by `ratelimit_remaining_header` and `ratelimit_reset_header`. Only requests using `HttpRequester` are rate-limited; custom components that bypass `HttpRequester` are not covered by this budget.

 */
export interface HTTPAPIBudget {
  type: HTTPAPIBudgetType;
  /** List of call rate policies that define how many calls are allowed. */
  policies: HTTPAPIBudgetPoliciesItem[];
  /** The HTTP response header name that indicates when the rate limit resets. */
  ratelimit_reset_header?: string;
  /** The HTTP response header name that indicates the number of remaining allowed calls. */
  ratelimit_remaining_header?: string;
  /** List of HTTP status codes that indicate a rate limit has been hit. */
  status_codes_for_ratelimit_hit?: number[];
  [key: string]: unknown;
}

export type FixedWindowCallRatePolicyType =
  (typeof FixedWindowCallRatePolicyType)[keyof typeof FixedWindowCallRatePolicyType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FixedWindowCallRatePolicyType = {
  FixedWindowCallRatePolicy: "FixedWindowCallRatePolicy",
} as const;

/**
 * A policy that allows a fixed number of calls within a specific time window.
 */
export interface FixedWindowCallRatePolicy {
  type: FixedWindowCallRatePolicyType;
  /** The time interval for the rate limit window. */
  period: string;
  /** The maximum number of calls allowed within the period. */
  call_limit: number;
  /** List of matchers that define which requests this policy applies to. */
  matchers: HttpRequestRegexMatcher[];
  [key: string]: unknown;
}

export type MovingWindowCallRatePolicyType =
  (typeof MovingWindowCallRatePolicyType)[keyof typeof MovingWindowCallRatePolicyType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MovingWindowCallRatePolicyType = {
  MovingWindowCallRatePolicy: "MovingWindowCallRatePolicy",
} as const;

/**
 * A policy that allows a fixed number of calls within a moving time window.
 */
export interface MovingWindowCallRatePolicy {
  type: MovingWindowCallRatePolicyType;
  /** List of rates that define the call limits for different time intervals. */
  rates: Rate[];
  /** List of matchers that define which requests this policy applies to. */
  matchers: HttpRequestRegexMatcher[];
  [key: string]: unknown;
}

export type UnlimitedCallRatePolicyType =
  (typeof UnlimitedCallRatePolicyType)[keyof typeof UnlimitedCallRatePolicyType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const UnlimitedCallRatePolicyType = {
  UnlimitedCallRatePolicy: "UnlimitedCallRatePolicy",
} as const;

/**
 * A policy that allows unlimited calls for specific requests.
 */
export interface UnlimitedCallRatePolicy {
  type: UnlimitedCallRatePolicyType;
  /** List of matchers that define which requests this policy applies to. */
  matchers: HttpRequestRegexMatcher[];
  [key: string]: unknown;
}

/**
 * The maximum number of calls allowed within the interval.
 */
export type RateLimit = number | string;

/**
 * Defines a rate limit with a specific number of calls allowed within a time interval.
 */
export interface Rate {
  /** The maximum number of calls allowed within the interval. */
  limit: RateLimit;
  /** The time interval for the rate limit. */
  interval: string;
  [key: string]: unknown;
}

/**
 * The query parameters to match.
 */
export type HttpRequestRegexMatcherParams = { [key: string]: unknown };

/**
 * The headers to match.
 */
export type HttpRequestRegexMatcherHeaders = { [key: string]: unknown };

/**
 * Matches HTTP requests based on method, base URL, URL path pattern, query parameters, and headers. Use `url_base` to specify the scheme and host (without trailing slash) and `url_path_pattern` to apply a regex to the request path.

 */
export interface HttpRequestRegexMatcher {
  /** The HTTP method to match (e.g., GET, POST). */
  method?: string;
  /** The base URL (scheme and host, e.g. "https://api.example.com") to match. */
  url_base?: string;
  /** A regular expression pattern to match the URL path. */
  url_path_pattern?: string;
  /** The query parameters to match. */
  params?: HttpRequestRegexMatcherParams;
  /** The headers to match. */
  headers?: HttpRequestRegexMatcherHeaders;
  [key: string]: unknown;
}

export type DefaultErrorHandlerType = (typeof DefaultErrorHandlerType)[keyof typeof DefaultErrorHandlerType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DefaultErrorHandlerType = {
  DefaultErrorHandler: "DefaultErrorHandler",
} as const;

export type DefaultErrorHandlerBackoffStrategiesItem =
  | ConstantBackoffStrategy
  | ExponentialBackoffStrategy
  | WaitTimeFromHeader
  | WaitUntilTimeFromHeader
  | CustomBackoffStrategy;

export type DefaultErrorHandlerParameters = { [key: string]: unknown };

/**
 * Component defining how to handle errors. Default behavior includes only retrying server errors (HTTP 5XX) and too many requests (HTTP 429) with an exponential backoff.
 */
export interface DefaultErrorHandler {
  type: DefaultErrorHandlerType;
  /** List of backoff strategies to use to determine how long to wait before retrying a retryable request. */
  backoff_strategies?: DefaultErrorHandlerBackoffStrategiesItem[];
  /** The maximum number of time to retry a retryable request before giving up and failing. */
  max_retries?: number;
  /** List of response filters to iterate on when deciding how to handle an error. When using an array of multiple filters, the filters will be applied sequentially and the response will be selected if it matches any of the filter's predicate. */
  response_filters?: HttpResponseFilter[];
  $parameters?: DefaultErrorHandlerParameters;
}

export type DefaultPaginatorType = (typeof DefaultPaginatorType)[keyof typeof DefaultPaginatorType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DefaultPaginatorType = {
  DefaultPaginator: "DefaultPaginator",
} as const;

/**
 * Strategy defining how records are paginated.
 */
export type DefaultPaginatorPaginationStrategy =
  | PageIncrement
  | OffsetIncrement
  | CursorPagination
  | CustomPaginationStrategy;

/**
 * Inject the page token into the outgoing HTTP requests by inserting it into either the request URL path or a field on the request.
 */
export type DefaultPaginatorPageTokenOption = RequestOption | RequestPath;

export type DefaultPaginatorParameters = { [key: string]: unknown };

/**
 * Default pagination implementation to request pages of results with a fixed size until the pagination strategy no longer returns a next_page_token.
 */
export interface DefaultPaginator {
  type: DefaultPaginatorType;
  /** Strategy defining how records are paginated. */
  pagination_strategy: DefaultPaginatorPaginationStrategy;
  page_size_option?: RequestOption;
  /** Inject the page token into the outgoing HTTP requests by inserting it into either the request URL path or a field on the request. */
  page_token_option?: DefaultPaginatorPageTokenOption;
  $parameters?: DefaultPaginatorParameters;
}

export type DpathExtractorType = (typeof DpathExtractorType)[keyof typeof DpathExtractorType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DpathExtractorType = {
  DpathExtractor: "DpathExtractor",
} as const;

export type DpathExtractorParameters = { [key: string]: unknown };

/**
 * Record extractor that searches a decoded response over a path defined as an array of fields.
 */
export interface DpathExtractor {
  type: DpathExtractorType;
  /** List of potentially nested fields describing the full path of the field to extract. Use "*" to extract all values from an array. See more info in the [docs](https://docs.airbyte.com/connector-development/config-based/understanding-the-yaml-file/record-selector). */
  field_path: string[];
  $parameters?: DpathExtractorParameters;
}

export type ResponseToFileExtractorType =
  (typeof ResponseToFileExtractorType)[keyof typeof ResponseToFileExtractorType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ResponseToFileExtractorType = {
  ResponseToFileExtractor: "ResponseToFileExtractor",
} as const;

export type ResponseToFileExtractorParameters = { [key: string]: unknown };

/**
 * A record extractor designed for handling large responses that may exceed memory limits (to prevent OOM issues). It downloads a CSV file to disk, reads the data from disk, and deletes the file once it has been fully processed.
 */
export interface ResponseToFileExtractor {
  type: ResponseToFileExtractorType;
  $parameters?: ResponseToFileExtractorParameters;
}

export type ExponentialBackoffStrategyType =
  (typeof ExponentialBackoffStrategyType)[keyof typeof ExponentialBackoffStrategyType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ExponentialBackoffStrategyType = {
  ExponentialBackoffStrategy: "ExponentialBackoffStrategy",
} as const;

/**
 * Multiplicative constant applied on each retry.
 */
export type ExponentialBackoffStrategyFactor = number | string;

export type ExponentialBackoffStrategyParameters = { [key: string]: unknown };

/**
 * Backoff strategy with an exponential backoff interval. The interval is defined as factor * 2^attempt_count.
 */
export interface ExponentialBackoffStrategy {
  type: ExponentialBackoffStrategyType;
  /** Multiplicative constant applied on each retry. */
  factor?: ExponentialBackoffStrategyFactor;
  $parameters?: ExponentialBackoffStrategyParameters;
}

export type GroupByKeyMergeStrategyType =
  (typeof GroupByKeyMergeStrategyType)[keyof typeof GroupByKeyMergeStrategyType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GroupByKeyMergeStrategyType = {
  GroupByKeyMergeStrategy: "GroupByKeyMergeStrategy",
} as const;

/**
 * The name of the field on the record whose value will be used to group properties that were retrieved through multiple API requests.
 */
export type GroupByKeyMergeStrategyKey = string | string[];

export type GroupByKeyMergeStrategyParameters = { [key: string]: unknown };

/**
 * Record merge strategy that combines records according to fields on the record.
 */
export interface GroupByKeyMergeStrategy {
  type: GroupByKeyMergeStrategyType;
  /** The name of the field on the record whose value will be used to group properties that were retrieved through multiple API requests. */
  key: GroupByKeyMergeStrategyKey;
  $parameters?: GroupByKeyMergeStrategyParameters;
}

export type SessionTokenAuthenticatorType =
  (typeof SessionTokenAuthenticatorType)[keyof typeof SessionTokenAuthenticatorType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SessionTokenAuthenticatorType = {
  SessionTokenAuthenticator: "SessionTokenAuthenticator",
} as const;

/**
 * Authentication method to use for requests sent to the API, specifying how to inject the session token.
 */
export type SessionTokenAuthenticatorRequestAuthentication =
  | SessionTokenRequestApiKeyAuthenticator
  | SessionTokenRequestBearerAuthenticator;

/**
 * Component used to decode the response.
 */
export type SessionTokenAuthenticatorDecoder = JsonDecoder | XmlDecoder;

export type SessionTokenAuthenticatorParameters = { [key: string]: unknown };

/**
 * Authenticator for requests using the session token as an API key that's injected into the request.
 */
export interface SessionTokenAuthenticator {
  type: SessionTokenAuthenticatorType;
  /** Description of the request to perform to obtain a session token to perform data requests. The response body is expected to be a JSON object with a session token property. */
  login_requester: HttpRequester;
  /** The path in the response body returned from the login requester to the session token. */
  session_token_path: string[];
  /** The duration in ISO 8601 duration notation after which the session token expires, starting from the time it was obtained. Omitting it will result in the session token being refreshed for every request.
   * **PT1H**: 1 hour
   * **P1D**: 1 day
   * **P1W**: 1 week
   * **P1M**: 1 month
   * **P1Y**: 1 year
   */
  expiration_duration?: string;
  /** Authentication method to use for requests sent to the API, specifying how to inject the session token. */
  request_authentication: SessionTokenAuthenticatorRequestAuthentication;
  /** Component used to decode the response. */
  decoder?: SessionTokenAuthenticatorDecoder;
  $parameters?: SessionTokenAuthenticatorParameters;
}

export type SessionTokenRequestApiKeyAuthenticatorType =
  (typeof SessionTokenRequestApiKeyAuthenticatorType)[keyof typeof SessionTokenRequestApiKeyAuthenticatorType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SessionTokenRequestApiKeyAuthenticatorType = {
  ApiKey: "ApiKey",
} as const;

/**
 * Authenticator for requests using the session token as an API key that's injected into the request.
 */
export interface SessionTokenRequestApiKeyAuthenticator {
  type: SessionTokenRequestApiKeyAuthenticatorType;
  /** Configure how the API Key will be sent in requests to the source API. */
  inject_into: RequestOption;
  /** A template for the token value to inject. Use {{ session_token }} to reference the session token. For example, use "Token {{ session_token }}" for APIs that expect "Authorization: Token <token>". */
  api_token?: string;
}

export type SessionTokenRequestBearerAuthenticatorType =
  (typeof SessionTokenRequestBearerAuthenticatorType)[keyof typeof SessionTokenRequestBearerAuthenticatorType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SessionTokenRequestBearerAuthenticatorType = {
  Bearer: "Bearer",
} as const;

/**
 * Authenticator for requests using the session token as a standard bearer token.
 */
export interface SessionTokenRequestBearerAuthenticator {
  type: SessionTokenRequestBearerAuthenticatorType;
}

export type HttpRequesterType = (typeof HttpRequesterType)[keyof typeof HttpRequesterType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const HttpRequesterType = {
  HttpRequester: "HttpRequester",
} as const;

/**
 * The HTTP method used to fetch data from the source (can be GET or POST).
 */
export type HttpRequesterHttpMethod = (typeof HttpRequesterHttpMethod)[keyof typeof HttpRequesterHttpMethod];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const HttpRequesterHttpMethod = {
  GET: "GET",
  POST: "POST",
} as const;

/**
 * Authentication method to use for requests sent to the API.
 */
export type HttpRequesterAuthenticator =
  | ApiKeyAuthenticator
  | BasicHttpAuthenticator
  | BearerAuthenticator
  | OAuthAuthenticator
  | JwtAuthenticator
  | SessionTokenAuthenticator
  | SelectiveAuthenticator
  | CustomAuthenticator
  | NoAuth
  | LegacySessionTokenAuthenticator;

export type HttpRequesterRequestParametersAnyOf = { [key: string]: string | QueryProperties };

/**
 * Specifies the query parameters that should be set on an outgoing HTTP request given the inputs.
 */
export type HttpRequesterRequestParameters = HttpRequesterRequestParametersAnyOf | string;

export type HttpRequesterRequestHeadersAnyOf = { [key: string]: string };

/**
 * Return any non-auth headers. Authentication headers will overwrite any overlapping headers returned from this method.
 */
export type HttpRequesterRequestHeaders = HttpRequesterRequestHeadersAnyOf | string;

export type HttpRequesterRequestBodyDataAnyOf = { [key: string]: string };

/**
 * Specifies how to populate the body of the request with a non-JSON payload. Plain text will be sent as is, whereas objects will be converted to a urlencoded form.
 * @deprecated
 */
export type HttpRequesterRequestBodyData = HttpRequesterRequestBodyDataAnyOf | string;

export type HttpRequesterRequestBodyJsonAnyOf = { [key: string]: unknown };

/**
 * Specifies how to populate the body of the request with a JSON payload. Can contain nested objects.
 * @deprecated
 */
export type HttpRequesterRequestBodyJson = HttpRequesterRequestBodyJsonAnyOf | string;

/**
 * Specifies how to populate the body of the request with a payload. Can contain nested objects.
 */
export type HttpRequesterRequestBody =
  | RequestBodyPlainText
  | RequestBodyUrlEncodedForm
  | RequestBodyJsonObject
  | RequestBodyGraphQL;

/**
 * Error handler component that defines how to handle errors.
 */
export type HttpRequesterErrorHandler = DefaultErrorHandler | CompositeErrorHandler | CustomErrorHandler;

export type HttpRequesterParameters = { [key: string]: unknown };

/**
 * Requester submitting HTTP requests and extracting records from the response.
 */
export interface HttpRequester {
  type: HttpRequesterType;
  /**
   * Deprecated, use the `url` instead. Base URL of the API source. Do not put sensitive information (e.g. API tokens) into this field - Use the Authenticator component for this.
   * @deprecated
   */
  url_base?: string;
  /** The URL of the source API endpoint. Do not put sensitive information (e.g. API tokens) into this field - Use the Authenticator component for this. */
  url?: string;
  /**
   * Deprecated, use the `url` instead. Path the specific API endpoint that this stream represents. Do not put sensitive information (e.g. API tokens) into this field - Use the Authenticator component for this.
   * @deprecated
   */
  path?: string;
  /** The HTTP method used to fetch data from the source (can be GET or POST). */
  http_method?: HttpRequesterHttpMethod;
  /** Authentication method to use for requests sent to the API. */
  authenticator?: HttpRequesterAuthenticator;
  /**
   * Allows for retrieving a dynamic set of properties from an API endpoint which can be injected into outbound request using the stream_partition.extra_fields.
   * @deprecated
   */
  fetch_properties_from_endpoint?: PropertiesFromEndpoint;
  /** For APIs that require explicit specification of the properties to query for, this component will take a static or dynamic set of properties (which can be optionally split into chunks) and allow them to be injected into an outbound request by accessing stream_partition.extra_fields. */
  query_properties?: QueryProperties;
  /** Specifies the query parameters that should be set on an outgoing HTTP request given the inputs. */
  request_parameters?: HttpRequesterRequestParameters;
  /** Return any non-auth headers. Authentication headers will overwrite any overlapping headers returned from this method. */
  request_headers?: HttpRequesterRequestHeaders;
  /**
   * Specifies how to populate the body of the request with a non-JSON payload. Plain text will be sent as is, whereas objects will be converted to a urlencoded form.
   * @deprecated
   */
  request_body_data?: HttpRequesterRequestBodyData;
  /**
   * Specifies how to populate the body of the request with a JSON payload. Can contain nested objects.
   * @deprecated
   */
  request_body_json?: HttpRequesterRequestBodyJson;
  /** Specifies how to populate the body of the request with a payload. Can contain nested objects. */
  request_body?: HttpRequesterRequestBody;
  /** Error handler component that defines how to handle errors. */
  error_handler?: HttpRequesterErrorHandler;
  /** Enables stream requests caching. When set to true, repeated requests to the same URL will return cached responses. Parent streams automatically have caching enabled. Only set this to false if you are certain that caching should be disabled, as it may negatively impact performance when the same data is needed multiple times (e.g., for scroll-based pagination APIs where caching causes duplicate records). */
  use_cache?: boolean;
  $parameters?: HttpRequesterParameters;
}

export type HttpResponseFilterType = (typeof HttpResponseFilterType)[keyof typeof HttpResponseFilterType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const HttpResponseFilterType = {
  HttpResponseFilter: "HttpResponseFilter",
} as const;

/**
 * Action to execute if a response matches the filter.
 */
export type HttpResponseFilterAction = (typeof HttpResponseFilterAction)[keyof typeof HttpResponseFilterAction];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const HttpResponseFilterAction = {
  SUCCESS: "SUCCESS",
  FAIL: "FAIL",
  RETRY: "RETRY",
  IGNORE: "IGNORE",
  RESET_PAGINATION: "RESET_PAGINATION",
  RATE_LIMITED: "RATE_LIMITED",
  REFRESH_TOKEN_THEN_RETRY: "REFRESH_TOKEN_THEN_RETRY",
} as const;

/**
 * Failure type of traced exception if a response matches the filter.
 */
export type HttpResponseFilterFailureType =
  (typeof HttpResponseFilterFailureType)[keyof typeof HttpResponseFilterFailureType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const HttpResponseFilterFailureType = {
  system_error: "system_error",
  config_error: "config_error",
  transient_error: "transient_error",
} as const;

export type HttpResponseFilterParameters = { [key: string]: unknown };

/**
 * A filter that is used to select on properties of the HTTP response received. When used with additional filters, a response will be selected if it matches any of the filter's criteria.
 */
export interface HttpResponseFilter {
  type: HttpResponseFilterType;
  /** Action to execute if a response matches the filter. */
  action?: HttpResponseFilterAction;
  /** Failure type of traced exception if a response matches the filter. */
  failure_type?: HttpResponseFilterFailureType;
  /** Error Message to display if the response matches the filter. */
  error_message?: string;
  /** Match the response if its error message contains the substring. */
  error_message_contains?: string;
  /** Match the response if its HTTP code is included in this list. */
  http_codes?: number[];
  /** Match the response if the predicate evaluates to true. */
  predicate?: string;
  $parameters?: HttpResponseFilterParameters;
}

export type ComplexFieldTypeItems = string | ComplexFieldType;

/**
 * (This component is experimental. Use at your own risk.) Represents a complex field type.
 */
export interface ComplexFieldType {
  field_type: string;
  items?: ComplexFieldTypeItems;
}

export type TypesMapTargetType = string | string[] | ComplexFieldType;

export type TypesMapCurrentType = string | string[];

/**
 * (This component is experimental. Use at your own risk.) Represents a mapping between a current type and its corresponding target type.
 */
export interface TypesMap {
  target_type: TypesMapTargetType;
  current_type: TypesMapCurrentType;
  condition?: string;
}

export type SchemaTypeIdentifierType = (typeof SchemaTypeIdentifierType)[keyof typeof SchemaTypeIdentifierType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SchemaTypeIdentifierType = {
  SchemaTypeIdentifier: "SchemaTypeIdentifier",
} as const;

export type SchemaTypeIdentifierParameters = { [key: string]: unknown };

/**
 * (This component is experimental. Use at your own risk.) Identifies schema details for dynamic schema extraction and processing.
 */
export interface SchemaTypeIdentifier {
  type?: SchemaTypeIdentifierType;
  /** List of nested fields defining the schema field path to extract. Defaults to []. */
  schema_pointer?: string[];
  /** List of potentially nested fields describing the full path of the field key to extract. */
  key_pointer: string[];
  /** List of potentially nested fields describing the full path of the field type to extract. */
  type_pointer?: string[];
  types_mapping?: TypesMap[];
  $parameters?: SchemaTypeIdentifierParameters;
}

export type DynamicSchemaLoaderType = (typeof DynamicSchemaLoaderType)[keyof typeof DynamicSchemaLoaderType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DynamicSchemaLoaderType = {
  DynamicSchemaLoader: "DynamicSchemaLoader",
} as const;

/**
 * Component used to coordinate how records are extracted across stream slices and request pages.
 */
export type DynamicSchemaLoaderRetriever = SimpleRetriever | AsyncRetriever | CustomRetriever;

/**
 * Responsible for filtering fields to be added to json schema.
 */
export type DynamicSchemaLoaderSchemaFilter = RecordFilter | CustomRecordFilter;

export type DynamicSchemaLoaderSchemaTransformationsItem =
  | AddFields
  | RemoveFields
  | KeysToLower
  | KeysToSnakeCase
  | FlattenFields
  | DpathFlattenFields
  | KeysReplace
  | CustomTransformation;

export type DynamicSchemaLoaderParameters = { [key: string]: unknown };

/**
 * (This component is experimental. Use at your own risk.) Loads a schema by extracting data from retrieved records.
 */
export interface DynamicSchemaLoader {
  type: DynamicSchemaLoaderType;
  /** Component used to coordinate how records are extracted across stream slices and request pages. */
  retriever: DynamicSchemaLoaderRetriever;
  /** Responsible for filtering fields to be added to json schema. */
  schema_filter?: DynamicSchemaLoaderSchemaFilter;
  /** A list of transformations to be applied to the schema. */
  schema_transformations?: DynamicSchemaLoaderSchemaTransformationsItem[];
  schema_type_identifier: SchemaTypeIdentifier;
  $parameters?: DynamicSchemaLoaderParameters;
}

export type InlineSchemaLoaderType = (typeof InlineSchemaLoaderType)[keyof typeof InlineSchemaLoaderType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const InlineSchemaLoaderType = {
  InlineSchemaLoader: "InlineSchemaLoader",
} as const;

/**
 * Describes a streams' schema. Refer to the <a href="https://docs.airbyte.com/understanding-airbyte/supported-data-types/">Data Types documentation</a> for more details on which types are valid.
 */
export type InlineSchemaLoaderSchema = { [key: string]: unknown };

/**
 * Loads a schema that is defined directly in the manifest file.
 */
export interface InlineSchemaLoader {
  type: InlineSchemaLoaderType;
  /** Describes a streams' schema. Refer to the <a href="https://docs.airbyte.com/understanding-airbyte/supported-data-types/">Data Types documentation</a> for more details on which types are valid. */
  schema?: InlineSchemaLoaderSchema;
}

export type JsonFileSchemaLoaderType = (typeof JsonFileSchemaLoaderType)[keyof typeof JsonFileSchemaLoaderType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const JsonFileSchemaLoaderType = {
  JsonFileSchemaLoader: "JsonFileSchemaLoader",
} as const;

export type JsonFileSchemaLoaderParameters = { [key: string]: unknown };

/**
 * Loads the schema from a json file.
 * @deprecated
 */
export interface JsonFileSchemaLoader {
  type: JsonFileSchemaLoaderType;
  /** Path to the JSON file defining the schema. The path is relative to the connector module's root. */
  file_path?: string;
  $parameters?: JsonFileSchemaLoaderParameters;
}

export type JsonDecoderType = (typeof JsonDecoderType)[keyof typeof JsonDecoderType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const JsonDecoderType = {
  JsonDecoder: "JsonDecoder",
} as const;

/**
 * Select 'JSON' if the response is formatted as a JSON object.
 */
export interface JsonDecoder {
  type: JsonDecoderType;
}

export type JsonlDecoderType = (typeof JsonlDecoderType)[keyof typeof JsonlDecoderType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const JsonlDecoderType = {
  JsonlDecoder: "JsonlDecoder",
} as const;

/**
 * Select 'JSON Lines' if the response consists of JSON objects separated by new lines ('\n') in JSONL format.
 */
export interface JsonlDecoder {
  type: JsonlDecoderType;
}

export type JsonSchemaPropertySelectorType =
  (typeof JsonSchemaPropertySelectorType)[keyof typeof JsonSchemaPropertySelectorType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const JsonSchemaPropertySelectorType = {
  JsonSchemaPropertySelector: "JsonSchemaPropertySelector",
} as const;

export type JsonSchemaPropertySelectorTransformationsItem =
  | AddFields
  | RemoveFields
  | KeysToLower
  | KeysToSnakeCase
  | FlattenFields
  | DpathFlattenFields
  | KeysReplace
  | CustomTransformation;

export type JsonSchemaPropertySelectorParameters = { [key: string]: unknown };

/**
 * When configured, the JSON schema supplied in the catalog containing which columns are selected for the current stream will be used to reduce which query properties will be included in the outbound API request. This can improve the performance of API requests, especially for those requiring multiple requests to get a complete record.
 */
export interface JsonSchemaPropertySelector {
  type: JsonSchemaPropertySelectorType;
  /** A list of transformations to be applied on the customer configured schema that will be used to filter out unselected fields when specifying query properties for API requests. */
  transformations?: JsonSchemaPropertySelectorTransformationsItem[];
  $parameters?: JsonSchemaPropertySelectorParameters;
}

export type KeysToLowerType = (typeof KeysToLowerType)[keyof typeof KeysToLowerType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const KeysToLowerType = {
  KeysToLower: "KeysToLower",
} as const;

export type KeysToLowerParameters = { [key: string]: unknown };

/**
 * A transformation that renames all keys to lower case.
 */
export interface KeysToLower {
  type: KeysToLowerType;
  $parameters?: KeysToLowerParameters;
}

export type KeysToSnakeCaseType = (typeof KeysToSnakeCaseType)[keyof typeof KeysToSnakeCaseType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const KeysToSnakeCaseType = {
  KeysToSnakeCase: "KeysToSnakeCase",
} as const;

export type KeysToSnakeCaseParameters = { [key: string]: unknown };

/**
 * A transformation that renames all keys to snake case.
 */
export interface KeysToSnakeCase {
  type: KeysToSnakeCaseType;
  $parameters?: KeysToSnakeCaseParameters;
}

export type FlattenFieldsType = (typeof FlattenFieldsType)[keyof typeof FlattenFieldsType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const FlattenFieldsType = {
  FlattenFields: "FlattenFields",
} as const;

export type FlattenFieldsParameters = { [key: string]: unknown };

/**
 * A transformation that flatten record to single level format.
 */
export interface FlattenFields {
  type: FlattenFieldsType;
  /** Whether to flatten lists or leave it as is. Default is True. */
  flatten_lists?: boolean;
  $parameters?: FlattenFieldsParameters;
}

export type KeyTransformationType = (typeof KeyTransformationType)[keyof typeof KeyTransformationType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const KeyTransformationType = {
  KeyTransformation: "KeyTransformation",
} as const;

export interface KeyTransformation {
  type: KeyTransformationType;
  /** Prefix to add for object keys. If not provided original keys remain unchanged. */
  prefix?: string;
  /** Suffix to add for object keys. If not provided original keys remain unchanged. */
  suffix?: string;
}

export type DpathFlattenFieldsType = (typeof DpathFlattenFieldsType)[keyof typeof DpathFlattenFieldsType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DpathFlattenFieldsType = {
  DpathFlattenFields: "DpathFlattenFields",
} as const;

export type DpathFlattenFieldsParameters = { [key: string]: unknown };

/**
 * A transformation that flatten field values to the to top of the record.
 */
export interface DpathFlattenFields {
  type: DpathFlattenFieldsType;
  /** A path to field that needs to be flattened. */
  field_path: string[];
  /** Whether to delete the origin value or keep it. Default is False. */
  delete_origin_value?: boolean;
  /** Whether to replace the origin record or not. Default is False. */
  replace_record?: boolean;
  /** Transformation for object keys. If not provided, original key will be used. */
  key_transformation?: KeyTransformation;
  $parameters?: DpathFlattenFieldsParameters;
}

export type KeysReplaceType = (typeof KeysReplaceType)[keyof typeof KeysReplaceType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const KeysReplaceType = {
  KeysReplace: "KeysReplace",
} as const;

export type KeysReplaceParameters = { [key: string]: unknown };

/**
 * A transformation that replaces symbols in keys.
 */
export interface KeysReplace {
  type: KeysReplaceType;
  /** Old value to replace. */
  old: string;
  /** New value to set. */
  new: string;
  $parameters?: KeysReplaceParameters;
}

export type IterableDecoderType = (typeof IterableDecoderType)[keyof typeof IterableDecoderType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const IterableDecoderType = {
  IterableDecoder: "IterableDecoder",
} as const;

/**
 * Select 'Iterable' if the response consists of strings separated by new lines (`\n`). The string will then be wrapped into a JSON object with the `record` key.
 */
export interface IterableDecoder {
  type: IterableDecoderType;
}

export type XmlDecoderType = (typeof XmlDecoderType)[keyof typeof XmlDecoderType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const XmlDecoderType = {
  XmlDecoder: "XmlDecoder",
} as const;

/**
 * Select 'XML' if the response consists of XML-formatted data.
 */
export interface XmlDecoder {
  type: XmlDecoderType;
}

export type CustomDecoderType = (typeof CustomDecoderType)[keyof typeof CustomDecoderType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomDecoderType = {
  CustomDecoder: "CustomDecoder",
} as const;

export type CustomDecoderParameters = { [key: string]: unknown };

/**
 * Use this to implement custom decoder logic.
 */
export interface CustomDecoder {
  type: CustomDecoderType;
  /** Fully-qualified name of the class that will be implementing the custom decoding. Has to be a sub class of Decoder. The format is `source_<name>.<package>.<class_name>`. */
  class_name: string;
  $parameters?: CustomDecoderParameters;
  [key: string]: unknown;
}

export type ZipfileDecoderType = (typeof ZipfileDecoderType)[keyof typeof ZipfileDecoderType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ZipfileDecoderType = {
  ZipfileDecoder: "ZipfileDecoder",
} as const;

/**
 * Parser to parse the decompressed data from the zipfile(s).
 */
export type ZipfileDecoderDecoder = CsvDecoder | GzipDecoder | JsonDecoder | JsonlDecoder;

/**
 * Select 'ZIP file' for response data that is returned as a zipfile. Requires specifying an inner data type/decoder to parse the unzipped data.
 */
export interface ZipfileDecoder {
  type: ZipfileDecoderType;
  /** Parser to parse the decompressed data from the zipfile(s). */
  decoder: ZipfileDecoderDecoder;
  [key: string]: unknown;
}

export type ListPartitionRouterType = (typeof ListPartitionRouterType)[keyof typeof ListPartitionRouterType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListPartitionRouterType = {
  ListPartitionRouter: "ListPartitionRouter",
} as const;

/**
 * The list of attributes being iterated over and used as input for the requests made to the source API.
 */
export type ListPartitionRouterValues = string | string[];

export type ListPartitionRouterParameters = { [key: string]: unknown };

/**
 * A Partition router that specifies a list of attributes where each attribute describes a portion of the complete data set for a stream. During a sync, each value is iterated over and can be used as input to outbound API requests.
 */
export interface ListPartitionRouter {
  type: ListPartitionRouterType;
  /** While iterating over list values, the name of field used to reference a list value. The partition value can be accessed with string interpolation. e.g. "{{ stream_partition['my_key'] }}" where "my_key" is the value of the cursor_field. */
  cursor_field: string;
  /** The list of attributes being iterated over and used as input for the requests made to the source API. */
  values: ListPartitionRouterValues;
  /** A request option describing where the list value should be injected into and under what field name if applicable. */
  request_option?: RequestOption;
  $parameters?: ListPartitionRouterParameters;
}

export type MinMaxDatetimeType = (typeof MinMaxDatetimeType)[keyof typeof MinMaxDatetimeType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const MinMaxDatetimeType = {
  MinMaxDatetime: "MinMaxDatetime",
} as const;

export type MinMaxDatetimeParameters = { [key: string]: unknown };

/**
 * Compares the provided date against optional minimum or maximum times. The max_datetime serves as the ceiling and will be returned when datetime exceeds it. The min_datetime serves as the floor.
 */
export interface MinMaxDatetime {
  type: MinMaxDatetimeType;
  /** Datetime value. */
  datetime: string;
  /** Format of the datetime value. Defaults to "%Y-%m-%dT%H:%M:%S.%f%z" if left empty. Use placeholders starting with "%" to describe the format the API is using. The following placeholders are available:
  * **%s**: Epoch unix timestamp - `1686218963`
  * **%s_as_float**: Epoch unix timestamp in seconds as float with microsecond precision - `1686218963.123456`
  * **%ms**: Epoch unix timestamp - `1686218963123`
  * **%a**: Weekday (abbreviated) - `Sun`
  * **%A**: Weekday (full) - `Sunday`
  * **%w**: Weekday (decimal) - `0` (Sunday), `6` (Saturday)
  * **%d**: Day of the month (zero-padded) - `01`, `02`, ..., `31`
  * **%b**: Month (abbreviated) - `Jan`
  * **%B**: Month (full) - `January`
  * **%m**: Month (zero-padded) - `01`, `02`, ..., `12`
  * **%y**: Year (without century, zero-padded) - `00`, `01`, ..., `99`
  * **%Y**: Year (with century) - `0001`, `0002`, ..., `9999`
  * **%H**: Hour (24-hour, zero-padded) - `00`, `01`, ..., `23`
  * **%I**: Hour (12-hour, zero-padded) - `01`, `02`, ..., `12`
  * **%p**: AM/PM indicator
  * **%M**: Minute (zero-padded) - `00`, `01`, ..., `59`
  * **%S**: Second (zero-padded) - `00`, `01`, ..., `59`
  * **%f**: Microsecond (zero-padded to 6 digits) - `000000`, `000001`, ..., `999999`
  * **%_ms**: Millisecond (zero-padded to 3 digits) - `000`, `001`, ..., `999`
  * **%z**: UTC offset - `(empty)`, `+0000`, `-04:00`
  * **%Z**: Time zone name - `(empty)`, `UTC`, `GMT`
  * **%j**: Day of the year (zero-padded) - `001`, `002`, ..., `366`
  * **%U**: Week number of the year (Sunday as first day) - `00`, `01`, ..., `53`
  * **%W**: Week number of the year (Monday as first day) - `00`, `01`, ..., `53`
  * **%c**: Date and time representation - `Tue Aug 16 21:30:00 1988`
  * **%x**: Date representation - `08/16/1988`
  * **%X**: Time representation - `21:30:00`
  * **%%**: Literal '%' character

  Some placeholders depend on the locale of the underlying system - in most cases this locale is configured as en/US. For more information see the [Python documentation](https://docs.python.org/3/library/datetime.html#strftime-and-strptime-format-codes).
 */
  datetime_format?: string;
  /** Ceiling applied on the datetime value. Must be formatted with the datetime_format field. */
  max_datetime?: string;
  /** Floor applied on the datetime value. Must be formatted with the datetime_format field. */
  min_datetime?: string;
  $parameters?: MinMaxDatetimeParameters;
}

export type NoAuthType = (typeof NoAuthType)[keyof typeof NoAuthType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const NoAuthType = {
  NoAuth: "NoAuth",
} as const;

export type NoAuthParameters = { [key: string]: unknown };

/**
 * Authenticator for requests requiring no authentication.
 */
export interface NoAuth {
  type: NoAuthType;
  $parameters?: NoAuthParameters;
}

export type NoPaginationType = (typeof NoPaginationType)[keyof typeof NoPaginationType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const NoPaginationType = {
  NoPagination: "NoPagination",
} as const;

/**
 * Pagination implementation that never returns a next page.
 */
export interface NoPagination {
  type: NoPaginationType;
}

/**
 * OAuth specific blob. This is a Json Schema used to validate Json configurations used as input to OAuth.
Must be a valid non-nested JSON that refers to properties from ConnectorSpecification.connectionSpecification
using special annotation 'path_in_connector_config'.
These are input values the user is entering through the UI to authenticate to the connector, that might also shared
as inputs for syncing data via the connector.
Examples:
if no connector values is shared during oauth flow, oauth_user_input_from_connector_config_specification=[]
if connector values such as 'app_id' inside the top level are used to generate the API url for the oauth flow,
  oauth_user_input_from_connector_config_specification={
    app_id: {
      type: string
      path_in_connector_config: ['app_id']
    }
  }
if connector values such as 'info.app_id' nested inside another object are used to generate the API url for the oauth flow,
  oauth_user_input_from_connector_config_specification={
    app_id: {
      type: string
      path_in_connector_config: ['info', 'app_id']
    }
  }
 */
export type OAuthConfigSpecificationOauthUserInputFromConnectorConfigSpecification = { [key: string]: unknown };

/**
 * The DeclarativeOAuth Specific optional headers to inject while exchanging the `auth_code` to `access_token` during `completeOAuthFlow` step.
 */
export type OAuthConfigSpecificationOauthConnectorInputSpecificationAccessTokenHeaders = { [key: string]: unknown };

/**
 * The DeclarativeOAuth Specific optional query parameters to inject while exchanging the `auth_code` to `access_token` during `completeOAuthFlow` step.
When this property is provided, the query params will be encoded as `Json` and included in the outgoing API request.
 */
export type OAuthConfigSpecificationOauthConnectorInputSpecificationAccessTokenParams = { [key: string]: unknown };

/**
 * The DeclarativeOAuth Specific object to provide the criteria of how the `state` query param should be constructed,
including length and complexity.
 */
export type OAuthConfigSpecificationOauthConnectorInputSpecificationState = {
  min: number;
  max: number;
  [key: string]: unknown;
};

/**
 * The DeclarativeOAuth specific blob.
Pertains to the fields defined by the connector relating to the OAuth flow.

Interpolation capabilities:
- The variables placeholders are declared as `{{my_var}}`.
- The nested resolution variables like `{{ {{my_nested_var}} }}` is allowed as well.

- The allowed interpolation context is:
  + base64Encoder - encode to `base64`, {{ {{my_var_a}}:{{my_var_b}} | base64Encoder }}
  + base64Decorer - decode from `base64` encoded string, {{ {{my_string_variable_or_string_value}} | base64Decoder }}
  + urlEncoder - encode the input string to URL-like format, {{ https://test.host.com/endpoint | urlEncoder}}
  + urlDecorer - decode the input url-encoded string into text format, {{ urlDecoder:https%3A%2F%2Fairbyte.io | urlDecoder}}
  + codeChallengeS256 - get the `codeChallenge` encoded value to provide additional data-provider specific authorisation values, {{ {{state_value}} | codeChallengeS256 }}

Examples:
  - The TikTok Marketing DeclarativeOAuth spec:
  {
    "oauth_connector_input_specification": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
          "consent_url": "https://ads.tiktok.com/marketing_api/auth?{{client_id_key}}={{client_id_value}}&{{redirect_uri_key}}={{ {{redirect_uri_value}} | urlEncoder}}&{{state_key}}={{state_value}}",
          "access_token_url": "https://business-api.tiktok.com/open_api/v1.3/oauth2/access_token/",
          "access_token_params": {
              "{{ auth_code_key }}": "{{ auth_code_value }}",
              "{{ client_id_key }}": "{{ client_id_value }}",
              "{{ client_secret_key }}": "{{ client_secret_value }}"
          },
          "access_token_headers": {
              "Content-Type": "application/json",
              "Accept": "application/json"
          },
          "extract_output": ["data.access_token"],
          "client_id_key": "app_id",
          "client_secret_key": "secret",
          "auth_code_key": "auth_code"
      }
    }
  }
 */
export type OAuthConfigSpecificationOauthConnectorInputSpecification = {
  /** The DeclarativeOAuth Specific string URL string template to initiate the authentication.
The placeholders are replaced during the processing to provide neccessary values. */
  consent_url: string;
  /** The DeclarativeOAuth Specific string of the scopes needed to be grant for authenticated user. */
  scope?: string;
  /** The DeclarativeOAuth Specific URL templated string to obtain the `access_token`, `refresh_token` etc.
The placeholders are replaced during the processing to provide neccessary values. */
  access_token_url: string;
  /** The DeclarativeOAuth Specific optional headers to inject while exchanging the `auth_code` to `access_token` during `completeOAuthFlow` step. */
  access_token_headers?: OAuthConfigSpecificationOauthConnectorInputSpecificationAccessTokenHeaders;
  /** The DeclarativeOAuth Specific optional query parameters to inject while exchanging the `auth_code` to `access_token` during `completeOAuthFlow` step.
When this property is provided, the query params will be encoded as `Json` and included in the outgoing API request. */
  access_token_params?: OAuthConfigSpecificationOauthConnectorInputSpecificationAccessTokenParams;
  /** The DeclarativeOAuth Specific list of strings to indicate which keys should be extracted and returned back to the input config. */
  extract_output?: string[];
  /** The DeclarativeOAuth Specific object to provide the criteria of how the `state` query param should be constructed,
including length and complexity. */
  state?: OAuthConfigSpecificationOauthConnectorInputSpecificationState;
  /** The DeclarativeOAuth Specific optional override to provide the custom `client_id` key name, if required by data-provider. */
  client_id_key?: string;
  /** The DeclarativeOAuth Specific optional override to provide the custom `client_secret` key name, if required by data-provider. */
  client_secret_key?: string;
  /** The DeclarativeOAuth Specific optional override to provide the custom `scope` key name, if required by data-provider. */
  scope_key?: string;
  /** The DeclarativeOAuth Specific optional override to provide the custom `state` key name, if required by data-provider. */
  state_key?: string;
  /** The DeclarativeOAuth Specific optional override to provide the custom `code` key name to something like `auth_code` or `custom_auth_code`, if required by data-provider. */
  auth_code_key?: string;
  /** The DeclarativeOAuth Specific optional override to provide the custom `redirect_uri` key name to something like `callback_uri`, if required by data-provider. */
  redirect_uri_key?: string;
  [key: string]: unknown;
};

/**
 * OAuth specific blob. This is a Json Schema used to validate Json configurations produced by the OAuth flows as they are
returned by the distant OAuth APIs.
Must be a valid JSON describing the fields to merge back to `ConnectorSpecification.connectionSpecification`.
For each field, a special annotation `path_in_connector_config` can be specified to determine where to merge it,
Examples:
    complete_oauth_output_specification={
      refresh_token: {
        type: string,
        path_in_connector_config: ['credentials', 'refresh_token']
      }
    }
 */
export type OAuthConfigSpecificationCompleteOauthOutputSpecification = { [key: string]: unknown };

/**
 * OAuth specific blob. This is a Json Schema used to validate Json configurations persisted as Airbyte Server configurations.
Must be a valid non-nested JSON describing additional fields configured by the Airbyte Instance or Workspace Admins to be used by the
server when completing an OAuth flow (typically exchanging an auth code for refresh token).
Examples:
    complete_oauth_server_input_specification={
      client_id: {
        type: string
      },
      client_secret: {
        type: string
      }
    }
 */
export type OAuthConfigSpecificationCompleteOauthServerInputSpecification = { [key: string]: unknown };

/**
 * OAuth specific blob. This is a Json Schema used to validate Json configurations persisted as Airbyte Server configurations that
also need to be merged back into the connector configuration at runtime.
This is a subset configuration of `complete_oauth_server_input_specification` that filters fields out to retain only the ones that
are necessary for the connector to function with OAuth. (some fields could be used during oauth flows but not needed afterwards, therefore
they would be listed in the `complete_oauth_server_input_specification` but not `complete_oauth_server_output_specification`)
Must be a valid non-nested JSON describing additional fields configured by the Airbyte Instance or Workspace Admins to be used by the
connector when using OAuth flow APIs.
These fields are to be merged back to `ConnectorSpecification.connectionSpecification`.
For each field, a special annotation `path_in_connector_config` can be specified to determine where to merge it,
Examples:
      complete_oauth_server_output_specification={
        client_id: {
          type: string,
          path_in_connector_config: ['credentials', 'client_id']
        },
        client_secret: {
          type: string,
          path_in_connector_config: ['credentials', 'client_secret']
        }
      }
 */
export type OAuthConfigSpecificationCompleteOauthServerOutputSpecification = { [key: string]: unknown };

/**
 * Specification describing how an 'advanced' Auth flow would need to function.
 */
export interface OAuthConfigSpecification {
  /** OAuth specific blob. This is a Json Schema used to validate Json configurations used as input to OAuth.
Must be a valid non-nested JSON that refers to properties from ConnectorSpecification.connectionSpecification
using special annotation 'path_in_connector_config'.
These are input values the user is entering through the UI to authenticate to the connector, that might also shared
as inputs for syncing data via the connector.
Examples:
if no connector values is shared during oauth flow, oauth_user_input_from_connector_config_specification=[]
if connector values such as 'app_id' inside the top level are used to generate the API url for the oauth flow,
  oauth_user_input_from_connector_config_specification={
    app_id: {
      type: string
      path_in_connector_config: ['app_id']
    }
  }
if connector values such as 'info.app_id' nested inside another object are used to generate the API url for the oauth flow,
  oauth_user_input_from_connector_config_specification={
    app_id: {
      type: string
      path_in_connector_config: ['info', 'app_id']
    }
  } */
  oauth_user_input_from_connector_config_specification?: OAuthConfigSpecificationOauthUserInputFromConnectorConfigSpecification;
  /** The DeclarativeOAuth specific blob.
Pertains to the fields defined by the connector relating to the OAuth flow.

Interpolation capabilities:
- The variables placeholders are declared as `{{my_var}}`.
- The nested resolution variables like `{{ {{my_nested_var}} }}` is allowed as well.

- The allowed interpolation context is:
  + base64Encoder - encode to `base64`, {{ {{my_var_a}}:{{my_var_b}} | base64Encoder }}
  + base64Decorer - decode from `base64` encoded string, {{ {{my_string_variable_or_string_value}} | base64Decoder }}
  + urlEncoder - encode the input string to URL-like format, {{ https://test.host.com/endpoint | urlEncoder}}
  + urlDecorer - decode the input url-encoded string into text format, {{ urlDecoder:https%3A%2F%2Fairbyte.io | urlDecoder}}
  + codeChallengeS256 - get the `codeChallenge` encoded value to provide additional data-provider specific authorisation values, {{ {{state_value}} | codeChallengeS256 }}

Examples:
  - The TikTok Marketing DeclarativeOAuth spec:
  {
    "oauth_connector_input_specification": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
          "consent_url": "https://ads.tiktok.com/marketing_api/auth?{{client_id_key}}={{client_id_value}}&{{redirect_uri_key}}={{ {{redirect_uri_value}} | urlEncoder}}&{{state_key}}={{state_value}}",
          "access_token_url": "https://business-api.tiktok.com/open_api/v1.3/oauth2/access_token/",
          "access_token_params": {
              "{{ auth_code_key }}": "{{ auth_code_value }}",
              "{{ client_id_key }}": "{{ client_id_value }}",
              "{{ client_secret_key }}": "{{ client_secret_value }}"
          },
          "access_token_headers": {
              "Content-Type": "application/json",
              "Accept": "application/json"
          },
          "extract_output": ["data.access_token"],
          "client_id_key": "app_id",
          "client_secret_key": "secret",
          "auth_code_key": "auth_code"
      }
    }
  } */
  oauth_connector_input_specification?: OAuthConfigSpecificationOauthConnectorInputSpecification;
  /** OAuth specific blob. This is a Json Schema used to validate Json configurations produced by the OAuth flows as they are
returned by the distant OAuth APIs.
Must be a valid JSON describing the fields to merge back to `ConnectorSpecification.connectionSpecification`.
For each field, a special annotation `path_in_connector_config` can be specified to determine where to merge it,
Examples:
    complete_oauth_output_specification={
      refresh_token: {
        type: string,
        path_in_connector_config: ['credentials', 'refresh_token']
      }
    } */
  complete_oauth_output_specification?: OAuthConfigSpecificationCompleteOauthOutputSpecification;
  /** OAuth specific blob. This is a Json Schema used to validate Json configurations persisted as Airbyte Server configurations.
Must be a valid non-nested JSON describing additional fields configured by the Airbyte Instance or Workspace Admins to be used by the
server when completing an OAuth flow (typically exchanging an auth code for refresh token).
Examples:
    complete_oauth_server_input_specification={
      client_id: {
        type: string
      },
      client_secret: {
        type: string
      }
    } */
  complete_oauth_server_input_specification?: OAuthConfigSpecificationCompleteOauthServerInputSpecification;
  /** OAuth specific blob. This is a Json Schema used to validate Json configurations persisted as Airbyte Server configurations that
also need to be merged back into the connector configuration at runtime.
This is a subset configuration of `complete_oauth_server_input_specification` that filters fields out to retain only the ones that
are necessary for the connector to function with OAuth. (some fields could be used during oauth flows but not needed afterwards, therefore
they would be listed in the `complete_oauth_server_input_specification` but not `complete_oauth_server_output_specification`)
Must be a valid non-nested JSON describing additional fields configured by the Airbyte Instance or Workspace Admins to be used by the
connector when using OAuth flow APIs.
These fields are to be merged back to `ConnectorSpecification.connectionSpecification`.
For each field, a special annotation `path_in_connector_config` can be specified to determine where to merge it,
Examples:
      complete_oauth_server_output_specification={
        client_id: {
          type: string,
          path_in_connector_config: ['credentials', 'client_id']
        },
        client_secret: {
          type: string,
          path_in_connector_config: ['credentials', 'client_secret']
        }
      } */
  complete_oauth_server_output_specification?: OAuthConfigSpecificationCompleteOauthServerOutputSpecification;
  [key: string]: unknown;
}

export type OffsetIncrementType = (typeof OffsetIncrementType)[keyof typeof OffsetIncrementType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const OffsetIncrementType = {
  OffsetIncrement: "OffsetIncrement",
} as const;

/**
 * The number of records to include in each pages.
 */
export type OffsetIncrementPageSize = number | string;

export type OffsetIncrementParameters = { [key: string]: unknown };

/**
 * Pagination strategy that returns the number of records reads so far and returns it as the next page token.
 */
export interface OffsetIncrement {
  type: OffsetIncrementType;
  /** The number of records to include in each pages. */
  page_size?: OffsetIncrementPageSize;
  /** Using the `offset` with value `0` during the first request */
  inject_on_first_request?: boolean;
  $parameters?: OffsetIncrementParameters;
}

export type PageIncrementType = (typeof PageIncrementType)[keyof typeof PageIncrementType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const PageIncrementType = {
  PageIncrement: "PageIncrement",
} as const;

/**
 * The number of records to include in each pages.
 */
export type PageIncrementPageSize = number | string;

export type PageIncrementParameters = { [key: string]: unknown };

/**
 * Pagination strategy that returns the number of pages reads so far and returns it as the next page token.
 */
export interface PageIncrement {
  type: PageIncrementType;
  /** The number of records to include in each pages. */
  page_size?: PageIncrementPageSize;
  /** Index of the first page to request. */
  start_from_page?: number;
  /** Using the `page number` with value defined by `start_from_page` during the first request */
  inject_on_first_request?: boolean;
  $parameters?: PageIncrementParameters;
}

export type ParentStreamConfigType = (typeof ParentStreamConfigType)[keyof typeof ParentStreamConfigType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ParentStreamConfigType = {
  ParentStreamConfig: "ParentStreamConfig",
} as const;

/**
 * Reference to the parent stream.
 */
export type ParentStreamConfigStream = DeclarativeStream | StateDelegatingStream;

export type ParentStreamConfigParameters = { [key: string]: unknown };

/**
 * Describes how to construct partitions from the records retrieved from the parent stream..
 */
export interface ParentStreamConfig {
  type: ParentStreamConfigType;
  /** Reference to the parent stream. */
  stream: ParentStreamConfigStream;
  /** The primary key of records from the parent stream that will be used during the retrieval of records for the current substream. This parent identifier field is typically a characteristic of the child records being extracted from the source API. */
  parent_key: string;
  /** While iterating over parent records during a sync, the parent_key value can be referenced by using this field. */
  partition_field: string;
  /** A request option describing where the parent key value should be injected into and under what field name if applicable. */
  request_option?: RequestOption;
  /** Indicates whether the parent stream should be read incrementally based on updates in the child stream. */
  incremental_dependency?: boolean;
  /** If set, this will enable lazy reading, using the initial read of parent records to extract child records. */
  lazy_read_pointer?: string[];
  /** Array of field paths to include as additional fields in the stream slice. Each path is an array of strings representing keys to access fields in the respective parent record. Accessible via `stream_slice.extra_fields`. Missing fields are set to `None`. */
  extra_fields?: string[][];
  $parameters?: ParentStreamConfigParameters;
}

/**
 * The stream field to be used to distinguish unique records. Can either be a single field, an array of fields representing a composite key, or an array of arrays representing a composite key where the fields are nested fields.
 */
export type PrimaryKey = string | string[] | string[][];

export type PropertiesFromEndpointType = (typeof PropertiesFromEndpointType)[keyof typeof PropertiesFromEndpointType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const PropertiesFromEndpointType = {
  PropertiesFromEndpoint: "PropertiesFromEndpoint",
} as const;

/**
 * Requester component that describes how to fetch the properties to query from a remote API endpoint.
 */
export type PropertiesFromEndpointRetriever = SimpleRetriever | CustomRetriever;

export type PropertiesFromEndpointParameters = { [key: string]: unknown };

/**
 * Defines the behavior for fetching the list of properties from an API that will be loaded into the requests to extract records. Note that stream_slices can't be interpolated from this retriever.
 */
export interface PropertiesFromEndpoint {
  type: PropertiesFromEndpointType;
  /** Describes the path to the field that should be extracted */
  property_field_path: string[];
  /** Requester component that describes how to fetch the properties to query from a remote API endpoint. */
  retriever: PropertiesFromEndpointRetriever;
  $parameters?: PropertiesFromEndpointParameters;
}

export type PropertyChunkingType = (typeof PropertyChunkingType)[keyof typeof PropertyChunkingType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const PropertyChunkingType = {
  PropertyChunking: "PropertyChunking",
} as const;

/**
 * The type used to determine the maximum number of properties per chunk
 */
export type PropertyChunkingPropertyLimitType =
  (typeof PropertyChunkingPropertyLimitType)[keyof typeof PropertyChunkingPropertyLimitType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const PropertyChunkingPropertyLimitType = {
  characters: "characters",
  property_count: "property_count",
} as const;

export type PropertyChunkingParameters = { [key: string]: unknown };

/**
 * For APIs with restrictions on the amount of properties that can be requester per request, property chunking can be applied to make multiple requests with a subset of the properties.
 */
export interface PropertyChunking {
  type: PropertyChunkingType;
  /** The type used to determine the maximum number of properties per chunk */
  property_limit_type: PropertyChunkingPropertyLimitType;
  /** The maximum amount of properties that can be retrieved per request according to the limit type. */
  property_limit?: number;
  /** Dictates how to records that require multiple requests to get all properties should be emitted to the destination */
  record_merge_strategy?: GroupByKeyMergeStrategy;
  $parameters?: PropertyChunkingParameters;
}

export type QueryPropertiesType = (typeof QueryPropertiesType)[keyof typeof QueryPropertiesType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const QueryPropertiesType = {
  QueryProperties: "QueryProperties",
} as const;

/**
 * The set of properties that will be queried for in the outbound request. This can either be statically defined or dynamic based on an API endpoint
 */
export type QueryPropertiesPropertyList = string[] | PropertiesFromEndpoint;

export type QueryPropertiesParameters = { [key: string]: unknown };

/**
 * For APIs that require explicit specification of the properties to query for, this component specifies which property fields and how they are supplied to outbound requests.
 */
export interface QueryProperties {
  type: QueryPropertiesType;
  /** The set of properties that will be queried for in the outbound request. This can either be statically defined or dynamic based on an API endpoint */
  property_list: QueryPropertiesPropertyList;
  /** The list of properties that should be included in every set of properties when multiple chunks of properties are being requested. */
  always_include_properties?: string[];
  /** Defines how query properties will be grouped into smaller sets for APIs with limitations on the number of properties fetched per API request. */
  property_chunking?: PropertyChunking;
  /** Defines where to look for and which query properties that should be sent in outbound API requests. For example, you can specify that only the selected columns of a stream should be in the request. */
  property_selector?: JsonSchemaPropertySelector;
  $parameters?: QueryPropertiesParameters;
}

export type RecordFilterType = (typeof RecordFilterType)[keyof typeof RecordFilterType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RecordFilterType = {
  RecordFilter: "RecordFilter",
} as const;

export type RecordFilterParameters = { [key: string]: unknown };

/**
 * Filter applied on a list of records.
 */
export interface RecordFilter {
  type: RecordFilterType;
  /** The predicate to filter a record. Records will be removed if evaluated to False. */
  condition?: string;
  $parameters?: RecordFilterParameters;
}

export type RecordSelectorType = (typeof RecordSelectorType)[keyof typeof RecordSelectorType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RecordSelectorType = {
  RecordSelector: "RecordSelector",
} as const;

export type RecordSelectorExtractor = DpathExtractor | CustomRecordExtractor;

/**
 * Responsible for filtering records to be emitted by the Source.
 */
export type RecordSelectorRecordFilter = RecordFilter | CustomRecordFilter;

/**
 * Responsible for normalization according to the schema.
 */
export type RecordSelectorSchemaNormalization = SchemaNormalization | CustomSchemaNormalization;

export type RecordSelectorParameters = { [key: string]: unknown };

/**
 * Responsible for translating an HTTP response into a list of records by extracting records from the response and optionally filtering records based on a heuristic.
 */
export interface RecordSelector {
  type: RecordSelectorType;
  extractor: RecordSelectorExtractor;
  /** Responsible for filtering records to be emitted by the Source. */
  record_filter?: RecordSelectorRecordFilter;
  /** Responsible for normalization according to the schema. */
  schema_normalization?: RecordSelectorSchemaNormalization;
  /** If true, transformation will be applied before record filtering. */
  transform_before_filtering?: boolean;
  $parameters?: RecordSelectorParameters;
}

/**
 * Responsible for normalization according to the schema.
 */
export type SchemaNormalization = (typeof SchemaNormalization)[keyof typeof SchemaNormalization];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SchemaNormalization = {
  Default: "Default",
  None: "None",
} as const;

export type RemoveFieldsType = (typeof RemoveFieldsType)[keyof typeof RemoveFieldsType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RemoveFieldsType = {
  RemoveFields: "RemoveFields",
} as const;

/**
 * A transformation which removes fields from a record. The fields removed are designated using FieldPointers. During transformation, if a field or any of its parents does not exist in the record, no error is thrown.
 */
export interface RemoveFields {
  type: RemoveFieldsType;
  /** The predicate to filter a property by a property value. Property will be removed if it is empty OR expression is evaluated to True., */
  condition?: string;
  /** Array of paths defining the field to remove. Each item is an array whose field describe the path of a field to remove. */
  field_pointers: string[][];
}

export type RequestPathType = (typeof RequestPathType)[keyof typeof RequestPathType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RequestPathType = {
  RequestPath: "RequestPath",
} as const;

/**
 * The URL path to be used for the HTTP request.
 */
export interface RequestPath {
  type: RequestPathType;
}

export type RequestOptionType = (typeof RequestOptionType)[keyof typeof RequestOptionType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RequestOptionType = {
  RequestOption: "RequestOption",
} as const;

/**
 * Configures where the descriptor should be set on the HTTP requests. Note that request parameters that are already encoded in the URL path will not be duplicated.
 */
export type RequestOptionInjectInto = (typeof RequestOptionInjectInto)[keyof typeof RequestOptionInjectInto];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RequestOptionInjectInto = {
  request_parameter: "request_parameter",
  header: "header",
  body_data: "body_data",
  body_json: "body_json",
} as const;

/**
 * Specifies the key field or path and where in the request a component's value should be injected.
 */
export interface RequestOption {
  type: RequestOptionType;
  /** Configures where the descriptor should be set on the HTTP requests. Note that request parameters that are already encoded in the URL path will not be duplicated. */
  inject_into: RequestOptionInjectInto;
  /** Configures which key should be used in the location that the descriptor is being injected into. We hope to eventually deprecate this field in favor of `field_path` for all request_options, but must currently maintain it for backwards compatibility in the Builder. */
  field_name?: string;
  /** Configures a path to be used for nested structures in JSON body requests (e.g. GraphQL queries) */
  field_path?: string[];
}

/**
 * The stream schemas representing the shape of the data emitted by the stream.
 */
export interface Schemas {
  [key: string]: unknown;
}

export type LegacySessionTokenAuthenticatorType =
  (typeof LegacySessionTokenAuthenticatorType)[keyof typeof LegacySessionTokenAuthenticatorType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const LegacySessionTokenAuthenticatorType = {
  LegacySessionTokenAuthenticator: "LegacySessionTokenAuthenticator",
} as const;

export type LegacySessionTokenAuthenticatorParameters = { [key: string]: unknown };

/**
 * Deprecated - use SessionTokenAuthenticator instead. Authenticator for requests authenticated using session tokens. A session token is a random value generated by a server to identify a specific user for the duration of one interaction session.
 * @deprecated
 */
export interface LegacySessionTokenAuthenticator {
  type: LegacySessionTokenAuthenticatorType;
  /** The name of the session token header that will be injected in the request */
  header: string;
  /** Path of the login URL (do not include the base URL) */
  login_url: string;
  /** Session token to use if using a pre-defined token. Not needed if authenticating with username + password pair */
  session_token?: string;
  /** Name of the key of the session token to be extracted from the response */
  session_token_response_key: string;
  /** Username used to authenticate and obtain a session token */
  username?: string;
  /** Password used to authenticate and obtain a session token */
  password?: string;
  /** Path of the URL to use to validate that the session token is valid (do not include the base URL) */
  validate_session_url: string;
  $parameters?: LegacySessionTokenAuthenticatorParameters;
}

export type StateDelegatingStreamType = (typeof StateDelegatingStreamType)[keyof typeof StateDelegatingStreamType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const StateDelegatingStreamType = {
  StateDelegatingStream: "StateDelegatingStream",
} as const;

export type StateDelegatingStreamParameters = { [key: string]: unknown };

/**
 * (This component is experimental. Use at your own risk.) Orchestrate the retriever's usage based on the state value.
 */
export interface StateDelegatingStream {
  type: StateDelegatingStreamType;
  /** The stream name. */
  name: string;
  /** Component used to coordinate how records are extracted across stream slices and request pages when the state is empty or not provided. */
  full_refresh_stream: DeclarativeStream;
  /** Component used to coordinate how records are extracted across stream slices and request pages when the state provided. */
  incremental_stream: DeclarativeStream;
  $parameters?: StateDelegatingStreamParameters;
}

export type SimpleRetrieverType = (typeof SimpleRetrieverType)[keyof typeof SimpleRetrieverType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SimpleRetrieverType = {
  SimpleRetriever: "SimpleRetriever",
} as const;

/**
 * Requester component that describes how to prepare HTTP requests to send to the source API.
 */
export type SimpleRetrieverRequester = HttpRequester | CustomRequester;

/**
 * Component decoding the response so records can be extracted.
 */
export type SimpleRetrieverDecoder =
  | JsonDecoder
  | XmlDecoder
  | CsvDecoder
  | JsonlDecoder
  | GzipDecoder
  | IterableDecoder
  | ZipfileDecoder
  | CustomDecoder;

/**
 * Paginator component that describes how to navigate through the API's pages.
 */
export type SimpleRetrieverPaginator = DefaultPaginator | NoPagination;

export type SimpleRetrieverPartitionRouterAnyOfItem =
  | SubstreamPartitionRouter
  | ListPartitionRouter
  | GroupingPartitionRouter
  | CustomPartitionRouter;

/**
 * Used to iteratively execute requests over a set of values, such as a parent stream's records or a list of constant values.
 */
export type SimpleRetrieverPartitionRouter =
  | SubstreamPartitionRouter
  | ListPartitionRouter
  | GroupingPartitionRouter
  | CustomPartitionRouter
  | SimpleRetrieverPartitionRouterAnyOfItem[];

export type SimpleRetrieverParameters = { [key: string]: unknown };

/**
 * Retrieves records by synchronously sending requests to fetch records. The retriever acts as an orchestrator between the requester, the record selector, the paginator, and the partition router.
 */
export interface SimpleRetriever {
  type: SimpleRetrieverType;
  /** Requester component that describes how to prepare HTTP requests to send to the source API. */
  requester: SimpleRetrieverRequester;
  /** Component decoding the response so records can be extracted. */
  decoder?: SimpleRetrieverDecoder;
  /** Component that describes how to extract records from a HTTP response. */
  record_selector: RecordSelector;
  /** Paginator component that describes how to navigate through the API's pages. */
  paginator?: SimpleRetrieverPaginator;
  /** Describes what triggers pagination reset and how to handle it. */
  pagination_reset?: PaginationReset;
  /** If true, the partition router and incremental request options will be ignored when paginating requests. Request options set directly on the requester will not be ignored. */
  ignore_stream_slicer_parameters_on_paginated_requests?: boolean;
  /** Used to iteratively execute requests over a set of values, such as a parent stream's records or a list of constant values. */
  partition_router?: SimpleRetrieverPartitionRouter;
  $parameters?: SimpleRetrieverParameters;
}

export type PaginationResetType = (typeof PaginationResetType)[keyof typeof PaginationResetType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const PaginationResetType = {
  PaginationReset: "PaginationReset",
} as const;

export type PaginationResetAction = (typeof PaginationResetAction)[keyof typeof PaginationResetAction];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const PaginationResetAction = {
  SPLIT_USING_CURSOR: "SPLIT_USING_CURSOR",
  RESET: "RESET",
} as const;

/**
 * Describes what triggers pagination reset and how to handle it. If SPLIT_USING_CURSOR, the connector developer is accountable for ensuring that the records are returned in ascending order.
 */
export interface PaginationReset {
  type: PaginationResetType;
  action: PaginationResetAction;
  limits?: PaginationResetLimits;
}

export type PaginationResetLimitsType = (typeof PaginationResetLimitsType)[keyof typeof PaginationResetLimitsType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const PaginationResetLimitsType = {
  PaginationResetLimits: "PaginationResetLimits",
} as const;

/**
 * Describes the limits that trigger pagination reset
 */
export interface PaginationResetLimits {
  type: PaginationResetLimitsType;
  number_of_records?: number;
}

export type GzipDecoderType = (typeof GzipDecoderType)[keyof typeof GzipDecoderType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GzipDecoderType = {
  GzipDecoder: "GzipDecoder",
} as const;

export type GzipDecoderDecoder = CsvDecoder | GzipDecoder | JsonDecoder | JsonlDecoder;

/**
 * Select 'gzip' for response data that is compressed with gzip. Requires specifying an inner data type/decoder to parse the decompressed data.
 */
export interface GzipDecoder {
  type: GzipDecoderType;
  decoder: GzipDecoderDecoder;
}

export type CsvDecoderType = (typeof CsvDecoderType)[keyof typeof CsvDecoderType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CsvDecoderType = {
  CsvDecoder: "CsvDecoder",
} as const;

/**
 * Select 'CSV' for response data that is formatted as CSV (comma-separated values). Can specify an encoding (default: 'utf-8') and a delimiter (default: ',').
 */
export interface CsvDecoder {
  type: CsvDecoderType;
  encoding?: string;
  delimiter?: string;
  set_values_to_none?: string[];
}

export type AsyncJobStatusMapType = (typeof AsyncJobStatusMapType)[keyof typeof AsyncJobStatusMapType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AsyncJobStatusMapType = {
  AsyncJobStatusMap: "AsyncJobStatusMap",
} as const;

/**
 * Matches the api job status to Async Job Status.
 */
export interface AsyncJobStatusMap {
  type?: AsyncJobStatusMapType;
  running: string[];
  completed: string[];
  failed: string[];
  timeout: string[];
}

export type AsyncRetrieverType = (typeof AsyncRetrieverType)[keyof typeof AsyncRetrieverType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const AsyncRetrieverType = {
  AsyncRetriever: "AsyncRetriever",
} as const;

/**
 * Responsible for fetching the actual status of the async job.
 */
export type AsyncRetrieverStatusExtractor = DpathExtractor | CustomRecordExtractor;

/**
 * Responsible for fetching the final result `urls` provided by the completed / finished / ready async job.
 */
export type AsyncRetrieverDownloadTargetExtractor = DpathExtractor | CustomRecordExtractor;

/**
 * Responsible for fetching the records from provided urls.
 */
export type AsyncRetrieverDownloadExtractor = DpathExtractor | CustomRecordExtractor | ResponseToFileExtractor;

/**
 * Requester component that describes how to prepare HTTP requests to send to the source API to create the async server-side job.
 */
export type AsyncRetrieverCreationRequester = HttpRequester | CustomRequester;

/**
 * Requester component that describes how to prepare HTTP requests to send to the source API to fetch the status of the running async job.
 */
export type AsyncRetrieverPollingRequester = HttpRequester | CustomRequester;

/**
 * The time in minutes after which the single Async Job should be considered as Timed Out.
 */
export type AsyncRetrieverPollingJobTimeout = number | string;

/**
 * Requester component that describes how to prepare HTTP requests to send to the source API to extract the url from polling response by the completed async job.
 */
export type AsyncRetrieverDownloadTargetRequester = HttpRequester | CustomRequester;

/**
 * Requester component that describes how to prepare HTTP requests to send to the source API to download the data provided by the completed async job.
 */
export type AsyncRetrieverDownloadRequester = HttpRequester | CustomRequester;

/**
 * Paginator component that describes how to navigate through the API's pages during download.
 */
export type AsyncRetrieverDownloadPaginator = DefaultPaginator | NoPagination;

/**
 * Requester component that describes how to prepare HTTP requests to send to the source API to abort a job once it is timed out from the source's perspective.
 */
export type AsyncRetrieverAbortRequester = HttpRequester | CustomRequester;

/**
 * Requester component that describes how to prepare HTTP requests to send to the source API to delete a job once the records are extracted.
 */
export type AsyncRetrieverDeleteRequester = HttpRequester | CustomRequester;

export type AsyncRetrieverPartitionRouterAnyOfItem =
  | ListPartitionRouter
  | SubstreamPartitionRouter
  | GroupingPartitionRouter
  | CustomPartitionRouter;

/**
 * PartitionRouter component that describes how to partition the stream, enabling incremental syncs and checkpointing.
 */
export type AsyncRetrieverPartitionRouter =
  | ListPartitionRouter
  | SubstreamPartitionRouter
  | GroupingPartitionRouter
  | CustomPartitionRouter
  | AsyncRetrieverPartitionRouterAnyOfItem[];

/**
 * Component decoding the response so records can be extracted.
 */
export type AsyncRetrieverDecoder =
  | CsvDecoder
  | GzipDecoder
  | JsonDecoder
  | JsonlDecoder
  | IterableDecoder
  | XmlDecoder
  | ZipfileDecoder
  | CustomDecoder;

/**
 * Component decoding the download response so records can be extracted.
 */
export type AsyncRetrieverDownloadDecoder =
  | CsvDecoder
  | GzipDecoder
  | JsonDecoder
  | JsonlDecoder
  | IterableDecoder
  | XmlDecoder
  | ZipfileDecoder
  | CustomDecoder;

export type AsyncRetrieverParameters = { [key: string]: unknown };

/**
 * Retrieves records by Asynchronously sending requests to fetch records. The retriever acts as an orchestrator between the requester, the record selector, the paginator, and the partition router.
 */
export interface AsyncRetriever {
  type: AsyncRetrieverType;
  /** Component that describes how to extract records from a HTTP response. */
  record_selector: RecordSelector;
  /** Async Job Status to Airbyte CDK Async Job Status mapping. */
  status_mapping: AsyncJobStatusMap;
  /** Responsible for fetching the actual status of the async job. */
  status_extractor: AsyncRetrieverStatusExtractor;
  /** Responsible for fetching the final result `urls` provided by the completed / finished / ready async job. */
  download_target_extractor?: AsyncRetrieverDownloadTargetExtractor;
  /** Responsible for fetching the records from provided urls. */
  download_extractor?: AsyncRetrieverDownloadExtractor;
  /** Requester component that describes how to prepare HTTP requests to send to the source API to create the async server-side job. */
  creation_requester: AsyncRetrieverCreationRequester;
  /** Requester component that describes how to prepare HTTP requests to send to the source API to fetch the status of the running async job. */
  polling_requester: AsyncRetrieverPollingRequester;
  /** The time in minutes after which the single Async Job should be considered as Timed Out. */
  polling_job_timeout?: AsyncRetrieverPollingJobTimeout;
  /** Requester component that describes how to prepare HTTP requests to send to the source API to extract the url from polling response by the completed async job. */
  download_target_requester?: AsyncRetrieverDownloadTargetRequester;
  /** Requester component that describes how to prepare HTTP requests to send to the source API to download the data provided by the completed async job. */
  download_requester: AsyncRetrieverDownloadRequester;
  /** Paginator component that describes how to navigate through the API's pages during download. */
  download_paginator?: AsyncRetrieverDownloadPaginator;
  /** Requester component that describes how to prepare HTTP requests to send to the source API to abort a job once it is timed out from the source's perspective. */
  abort_requester?: AsyncRetrieverAbortRequester;
  /** Requester component that describes how to prepare HTTP requests to send to the source API to delete a job once the records are extracted. */
  delete_requester?: AsyncRetrieverDeleteRequester;
  /** PartitionRouter component that describes how to partition the stream, enabling incremental syncs and checkpointing. */
  partition_router?: AsyncRetrieverPartitionRouter;
  /** Component decoding the response so records can be extracted. */
  decoder?: AsyncRetrieverDecoder;
  /** Component decoding the download response so records can be extracted. */
  download_decoder?: AsyncRetrieverDownloadDecoder;
  $parameters?: AsyncRetrieverParameters;
}

export type SpecType = (typeof SpecType)[keyof typeof SpecType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SpecType = {
  Spec: "Spec",
} as const;

/**
 * A connection specification describing how a the connector can be configured.
 */
export type SpecConnectionSpecification = { [key: string]: unknown };

export type SpecConfigNormalizationRulesType =
  (typeof SpecConfigNormalizationRulesType)[keyof typeof SpecConfigNormalizationRulesType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SpecConfigNormalizationRulesType = {
  ConfigNormalizationRules: "ConfigNormalizationRules",
} as const;

export type SpecConfigNormalizationRulesTransformationsItem =
  | ConfigRemapField
  | ConfigAddFields
  | ConfigRemoveFields
  | CustomConfigTransformation;

export type SpecConfigNormalizationRulesValidationsItem = DpathValidator | PredicateValidator;

export type SpecConfigNormalizationRules = {
  type: SpecConfigNormalizationRulesType;
  /** The discrete migrations that will be applied on the incoming config. Each migration will be applied in the order they are defined. */
  config_migrations?: ConfigMigration[];
  /** The list of transformations that will be applied on the incoming config at the start of each sync. The transformations will be applied in the order they are defined. */
  transformations?: SpecConfigNormalizationRulesTransformationsItem[];
  /** The list of validations that will be performed on the incoming config at the start of each sync. */
  validations?: SpecConfigNormalizationRulesValidationsItem[];
};

/**
 * A source specification made up of connector metadata and how it can be configured.
 */
export interface Spec {
  type: SpecType;
  /** A connection specification describing how a the connector can be configured. */
  connection_specification: SpecConnectionSpecification;
  /** URL of the connector's documentation page. */
  documentation_url?: string;
  /** Advanced specification for configuring the authentication flow. */
  advanced_auth?: AuthFlow;
  config_normalization_rules?: SpecConfigNormalizationRules;
}

export type ConfigMigrationType = (typeof ConfigMigrationType)[keyof typeof ConfigMigrationType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ConfigMigrationType = {
  ConfigMigration: "ConfigMigration",
} as const;

export type ConfigMigrationTransformationsItem =
  | ConfigRemapField
  | ConfigAddFields
  | ConfigRemoveFields
  | CustomConfigTransformation;

/**
 * A config migration that will be applied on the incoming config at the start of a sync.
 */
export interface ConfigMigration {
  type: ConfigMigrationType;
  /** The description/purpose of the config migration. */
  description?: string;
  /** The list of transformations that will attempt to be applied on an incoming unmigrated config. The transformations will be applied in the order they are defined. */
  transformations: ConfigMigrationTransformationsItem[];
}

export type SubstreamPartitionRouterType =
  (typeof SubstreamPartitionRouterType)[keyof typeof SubstreamPartitionRouterType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const SubstreamPartitionRouterType = {
  SubstreamPartitionRouter: "SubstreamPartitionRouter",
} as const;

export type SubstreamPartitionRouterParameters = { [key: string]: unknown };

/**
 * Partition router that is used to retrieve records that have been partitioned according to records from the specified parent streams. An example of a parent stream is automobile brands and the substream would be the various car models associated with each branch.
 */
export interface SubstreamPartitionRouter {
  type: SubstreamPartitionRouterType;
  /** Specifies which parent streams are being iterated over and how parent records should be used to partition the child stream data set. */
  parent_stream_configs: ParentStreamConfig[];
  $parameters?: SubstreamPartitionRouterParameters;
}

/**
 * A schema type.
 */
export type ValueType = (typeof ValueType)[keyof typeof ValueType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ValueType = {
  string: "string",
  number: "number",
  integer: "integer",
  boolean: "boolean",
} as const;

export type WaitTimeFromHeaderType = (typeof WaitTimeFromHeaderType)[keyof typeof WaitTimeFromHeaderType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WaitTimeFromHeaderType = {
  WaitTimeFromHeader: "WaitTimeFromHeader",
} as const;

export type WaitTimeFromHeaderParameters = { [key: string]: unknown };

/**
 * Extract wait time from a HTTP header in the response.
 */
export interface WaitTimeFromHeader {
  type: WaitTimeFromHeaderType;
  /** The name of the response header defining how long to wait before retrying. */
  header: string;
  /** Optional regex to apply on the header to extract its value. The regex should define a capture group defining the wait time. */
  regex?: string;
  /** Given the value extracted from the header is greater than this value, stop the stream. */
  max_waiting_time_in_seconds?: number;
  $parameters?: WaitTimeFromHeaderParameters;
}

export type GroupingPartitionRouterType =
  (typeof GroupingPartitionRouterType)[keyof typeof GroupingPartitionRouterType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const GroupingPartitionRouterType = {
  GroupingPartitionRouter: "GroupingPartitionRouter",
} as const;

/**
 * The partition router whose output will be grouped. This can be any valid partition router component.
 */
export type GroupingPartitionRouterUnderlyingPartitionRouter =
  | ListPartitionRouter
  | SubstreamPartitionRouter
  | CustomPartitionRouter;

export type GroupingPartitionRouterParameters = { [key: string]: unknown };

/**
 * A decorator on top of a partition router that groups partitions into batches of a specified size. This is useful for APIs that support filtering by multiple partition keys in a single request. Note that per-partition incremental syncs may not work as expected because the grouping of partitions might change between syncs, potentially leading to inconsistent state tracking.

 */
export interface GroupingPartitionRouter {
  type: GroupingPartitionRouterType;
  /** The number of partitions to include in each group. This determines how many partition values are batched together in a single slice. */
  group_size: number;
  /** The partition router whose output will be grouped. This can be any valid partition router component. */
  underlying_partition_router: GroupingPartitionRouterUnderlyingPartitionRouter;
  /** If true, ensures that partitions are unique within each group by removing duplicates based on the partition key. */
  deduplicate?: boolean;
  $parameters?: GroupingPartitionRouterParameters;
}

export type WaitUntilTimeFromHeaderType =
  (typeof WaitUntilTimeFromHeaderType)[keyof typeof WaitUntilTimeFromHeaderType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const WaitUntilTimeFromHeaderType = {
  WaitUntilTimeFromHeader: "WaitUntilTimeFromHeader",
} as const;

/**
 * Minimum time to wait before retrying.
 */
export type WaitUntilTimeFromHeaderMinWait = number | string;

export type WaitUntilTimeFromHeaderParameters = { [key: string]: unknown };

/**
 * Extract time at which we can retry the request from response header and wait for the difference between now and that time.
 */
export interface WaitUntilTimeFromHeader {
  type: WaitUntilTimeFromHeaderType;
  /** The name of the response header defining how long to wait before retrying. */
  header: string;
  /** Minimum time to wait before retrying. */
  min_wait?: WaitUntilTimeFromHeaderMinWait;
  /** Optional regex to apply on the header to extract its value. The regex should define a capture group defining the wait time. */
  regex?: string;
  $parameters?: WaitUntilTimeFromHeaderParameters;
}

export type ComponentMappingDefinitionType =
  (typeof ComponentMappingDefinitionType)[keyof typeof ComponentMappingDefinitionType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ComponentMappingDefinitionType = {
  ComponentMappingDefinition: "ComponentMappingDefinition",
} as const;

export type ComponentMappingDefinitionParameters = { [key: string]: unknown };

/**
 * (This component is experimental. Use at your own risk.) Specifies a mapping definition to update or add fields in a record or configuration. This allows dynamic mapping of data by interpolating values into the template based on provided contexts.
 */
export interface ComponentMappingDefinition {
  type: ComponentMappingDefinitionType;
  /** A list of potentially nested fields indicating the full path where value will be added or updated. */
  field_path: string[];
  /** The dynamic or static value to assign to the key. Interpolated values can be used to dynamically determine the value during runtime. */
  value: string;
  /** The expected data type of the value. If omitted, the type will be inferred from the value provided. */
  value_type?: ValueType;
  /** Determines whether to create a new path if it doesn't exist (true) or only update existing paths (false). When set to true, the resolver will create new paths in the stream template if they don't exist. When false (default), it will only update existing paths. */
  create_or_update?: boolean;
  /** A condition that must be met for the mapping to be applied. This property is only supported for `ConfigComponentsResolver`. */
  condition?: string;
  $parameters?: ComponentMappingDefinitionParameters;
}

export type HttpComponentsResolverType = (typeof HttpComponentsResolverType)[keyof typeof HttpComponentsResolverType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const HttpComponentsResolverType = {
  HttpComponentsResolver: "HttpComponentsResolver",
} as const;

/**
 * Component used to coordinate how records are extracted across stream slices and request pages.
 */
export type HttpComponentsResolverRetriever = SimpleRetriever | AsyncRetriever | CustomRetriever;

export type HttpComponentsResolverParameters = { [key: string]: unknown };

/**
 * (This component is experimental. Use at your own risk.) Component resolve and populates stream templates with components fetched via an HTTP retriever.
 */
export interface HttpComponentsResolver {
  type: HttpComponentsResolverType;
  /** Component used to coordinate how records are extracted across stream slices and request pages. */
  retriever: HttpComponentsResolverRetriever;
  components_mapping: ComponentMappingDefinition[];
  $parameters?: HttpComponentsResolverParameters;
}

export type StreamConfigType = (typeof StreamConfigType)[keyof typeof StreamConfigType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const StreamConfigType = {
  StreamConfig: "StreamConfig",
} as const;

export type StreamConfigDefaultValuesItem = { [key: string]: unknown };

export type StreamConfigParameters = { [key: string]: unknown };

/**
 * (This component is experimental. Use at your own risk.) Describes how to get streams config from the source config.
 */
export interface StreamConfig {
  type: StreamConfigType;
  /** A list of potentially nested fields indicating the full path in source config file where streams configs located. */
  configs_pointer: string[];
  /** A list of default values, each matching the structure expected from the parsed component value. */
  default_values?: StreamConfigDefaultValuesItem[];
  $parameters?: StreamConfigParameters;
}

export type ConfigComponentsResolverType =
  (typeof ConfigComponentsResolverType)[keyof typeof ConfigComponentsResolverType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ConfigComponentsResolverType = {
  ConfigComponentsResolver: "ConfigComponentsResolver",
} as const;

export type ConfigComponentsResolverStreamConfig = StreamConfig[] | StreamConfig;

export type ConfigComponentsResolverParameters = { [key: string]: unknown };

/**
 * (This component is experimental. Use at your own risk.) Resolves and populates stream templates with components fetched from the source config.
 */
export interface ConfigComponentsResolver {
  type: ConfigComponentsResolverType;
  stream_config: ConfigComponentsResolverStreamConfig;
  components_mapping: ComponentMappingDefinition[];
  $parameters?: ConfigComponentsResolverParameters;
}

export type StreamParametersDefinitionType =
  (typeof StreamParametersDefinitionType)[keyof typeof StreamParametersDefinitionType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const StreamParametersDefinitionType = {
  StreamParametersDefinition: "StreamParametersDefinition",
} as const;

export type StreamParametersDefinitionListOfParametersForStreamItem = { [key: string]: unknown };

/**
 * (This component is experimental. Use at your own risk.) Represents a stream parameters definition to set up dynamic streams from defined values in manifest.
 */
export interface StreamParametersDefinition {
  type: StreamParametersDefinitionType;
  /** A list of object of parameters for stream, each object in the list represents params for one stream. */
  list_of_parameters_for_stream: StreamParametersDefinitionListOfParametersForStreamItem[];
}

export type ParametrizedComponentsResolverType =
  (typeof ParametrizedComponentsResolverType)[keyof typeof ParametrizedComponentsResolverType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ParametrizedComponentsResolverType = {
  ParametrizedComponentsResolver: "ParametrizedComponentsResolver",
} as const;

export type ParametrizedComponentsResolverParameters = { [key: string]: unknown };

/**
 * (This component is experimental. Use at your own risk.) Resolves and populates dynamic streams from defined parametrized values in manifest.
 */
export interface ParametrizedComponentsResolver {
  type: ParametrizedComponentsResolverType;
  stream_parameters: StreamParametersDefinition;
  components_mapping: ComponentMappingDefinition[];
  $parameters?: ParametrizedComponentsResolverParameters;
}

export type DynamicDeclarativeStreamType =
  (typeof DynamicDeclarativeStreamType)[keyof typeof DynamicDeclarativeStreamType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DynamicDeclarativeStreamType = {
  DynamicDeclarativeStream: "DynamicDeclarativeStream",
} as const;

/**
 * Reference to the stream template.
 */
export type DynamicDeclarativeStreamStreamTemplate = DeclarativeStream | StateDelegatingStream;

/**
 * Component resolve and populates stream templates with components values.
 */
export type DynamicDeclarativeStreamComponentsResolver =
  | HttpComponentsResolver
  | ConfigComponentsResolver
  | ParametrizedComponentsResolver;

/**
 * (This component is experimental. Use at your own risk.) A component that described how will be created declarative streams based on stream template.
 */
export interface DynamicDeclarativeStream {
  type: DynamicDeclarativeStreamType;
  /** The dynamic stream name. */
  name?: string;
  /** Reference to the stream template. */
  stream_template: DynamicDeclarativeStreamStreamTemplate;
  /** Component resolve and populates stream templates with components values. */
  components_resolver: DynamicDeclarativeStreamComponentsResolver;
  /** Whether or not to prioritize parent parameters over component parameters when constructing dynamic streams. Defaults to true for backward compatibility. */
  use_parent_parameters?: boolean;
}

export type RequestBodyPlainTextType = (typeof RequestBodyPlainTextType)[keyof typeof RequestBodyPlainTextType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RequestBodyPlainTextType = {
  RequestBodyPlainText: "RequestBodyPlainText",
} as const;

/**
 * Request body value is sent as plain text
 */
export interface RequestBodyPlainText {
  type: RequestBodyPlainTextType;
  value: string;
}

export type RequestBodyUrlEncodedFormType =
  (typeof RequestBodyUrlEncodedFormType)[keyof typeof RequestBodyUrlEncodedFormType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RequestBodyUrlEncodedFormType = {
  RequestBodyUrlEncodedForm: "RequestBodyUrlEncodedForm",
} as const;

export type RequestBodyUrlEncodedFormValue = { [key: string]: string };

/**
 * Request body value is converted into a url-encoded form
 */
export interface RequestBodyUrlEncodedForm {
  type: RequestBodyUrlEncodedFormType;
  value: RequestBodyUrlEncodedFormValue;
}

export type RequestBodyJsonObjectType = (typeof RequestBodyJsonObjectType)[keyof typeof RequestBodyJsonObjectType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RequestBodyJsonObjectType = {
  RequestBodyJsonObject: "RequestBodyJsonObject",
} as const;

export type RequestBodyJsonObjectValue = { [key: string]: unknown };

/**
 * Request body value converted into a JSON object
 */
export interface RequestBodyJsonObject {
  type: RequestBodyJsonObjectType;
  value: RequestBodyJsonObjectValue;
}

export type RequestBodyGraphQLType = (typeof RequestBodyGraphQLType)[keyof typeof RequestBodyGraphQLType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const RequestBodyGraphQLType = {
  RequestBodyGraphQL: "RequestBodyGraphQL",
} as const;

/**
 * Request body value converted into a GraphQL query object
 */
export interface RequestBodyGraphQL {
  type: RequestBodyGraphQLType;
  value: RequestBodyGraphQlQuery;
}

/**
 * Request body GraphQL query object
 */
export interface RequestBodyGraphQlQuery {
  /** The GraphQL query to be executed */
  query: string;
  [key: string]: unknown;
}

export type DpathValidatorType = (typeof DpathValidatorType)[keyof typeof DpathValidatorType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const DpathValidatorType = {
  DpathValidator: "DpathValidator",
} as const;

/**
 * The condition that the specified config value will be evaluated against
 */
export type DpathValidatorValidationStrategy = ValidateAdheresToSchema | CustomValidationStrategy;

/**
 * Validator that extracts the value located at a given field path.
 */
export interface DpathValidator {
  type: DpathValidatorType;
  /** List of potentially nested fields describing the full path of the field to validate. Use "*" to validate all values from an array. */
  field_path: string[];
  /** The condition that the specified config value will be evaluated against */
  validation_strategy: DpathValidatorValidationStrategy;
}

export type PredicateValidatorType = (typeof PredicateValidatorType)[keyof typeof PredicateValidatorType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const PredicateValidatorType = {
  PredicateValidator: "PredicateValidator",
} as const;

/**
 * The value to be validated. Can be a literal value or interpolated from configuration.
 */
export type PredicateValidatorValueAnyOf = { [key: string]: unknown };

/**
 * The value to be validated. Can be a literal value or interpolated from configuration.
 */
export type PredicateValidatorValue = string | number | PredicateValidatorValueAnyOf | boolean | null;

/**
 * The validation strategy to apply to the value.
 */
export type PredicateValidatorValidationStrategy = ValidateAdheresToSchema | CustomValidationStrategy;

/**
 * Validator that applies a validation strategy to a specified value.
 */
export interface PredicateValidator {
  type: PredicateValidatorType;
  /** The value to be validated. Can be a literal value or interpolated from configuration. */
  value: PredicateValidatorValue;
  /** The validation strategy to apply to the value. */
  validation_strategy: PredicateValidatorValidationStrategy;
}

export type ValidateAdheresToSchemaType =
  (typeof ValidateAdheresToSchemaType)[keyof typeof ValidateAdheresToSchemaType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ValidateAdheresToSchemaType = {
  ValidateAdheresToSchema: "ValidateAdheresToSchema",
} as const;

/**
 * The base JSON schema against which the user-provided schema will be validated.
 */
export type ValidateAdheresToSchemaBaseSchemaAnyOf = { [key: string]: unknown };

/**
 * The base JSON schema against which the user-provided schema will be validated.
 */
export type ValidateAdheresToSchemaBaseSchema = string | ValidateAdheresToSchemaBaseSchemaAnyOf;

/**
 * Validates that a user-provided schema adheres to a specified JSON schema.
 */
export interface ValidateAdheresToSchema {
  type: ValidateAdheresToSchemaType;
  /** The base JSON schema against which the user-provided schema will be validated. */
  base_schema: ValidateAdheresToSchemaBaseSchema;
}

export type CustomValidationStrategyType =
  (typeof CustomValidationStrategyType)[keyof typeof CustomValidationStrategyType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomValidationStrategyType = {
  CustomValidationStrategy: "CustomValidationStrategy",
} as const;

/**
 * Custom validation strategy that allows for custom validation logic.
 */
export interface CustomValidationStrategy {
  type: CustomValidationStrategyType;
  /** Fully-qualified name of the class that will be implementing the custom validation strategy. Has to be a sub class of ValidationStrategy. The format is `source_<name>.<package>.<class_name>`. */
  class_name: string;
  [key: string]: unknown;
}

export type ConfigRemapFieldType = (typeof ConfigRemapFieldType)[keyof typeof ConfigRemapFieldType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ConfigRemapFieldType = {
  ConfigRemapField: "ConfigRemapField",
} as const;

/**
 * A mapping of original values to new values. When a field value matches a key in this map, it will be replaced with the corresponding value.
 */
export type ConfigRemapFieldMapAnyOf = { [key: string]: unknown };

/**
 * A mapping of original values to new values. When a field value matches a key in this map, it will be replaced with the corresponding value.
 */
export type ConfigRemapFieldMap = ConfigRemapFieldMapAnyOf | string;

/**
 * Transformation that remaps a field's value to another value based on a static map.
 */
export interface ConfigRemapField {
  type: ConfigRemapFieldType;
  /** A mapping of original values to new values. When a field value matches a key in this map, it will be replaced with the corresponding value. */
  map: ConfigRemapFieldMap;
  /** The path to the field whose value should be remapped. Specified as a list of path components to navigate through nested objects. */
  field_path: string[];
}

export type ConfigAddFieldsType = (typeof ConfigAddFieldsType)[keyof typeof ConfigAddFieldsType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ConfigAddFieldsType = {
  ConfigAddFields: "ConfigAddFields",
} as const;

/**
 * Transformation that adds fields to a config. The path of the added field can be nested.
 */
export interface ConfigAddFields {
  type: ConfigAddFieldsType;
  /** A list of transformations (path and corresponding value) that will be added to the config. */
  fields: AddedFieldDefinition[];
  /** Fields will be added if expression is evaluated to True. */
  condition?: string;
}

export type ConfigRemoveFieldsType = (typeof ConfigRemoveFieldsType)[keyof typeof ConfigRemoveFieldsType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ConfigRemoveFieldsType = {
  ConfigRemoveFields: "ConfigRemoveFields",
} as const;

/**
 * Transformation that removes a field from the config.
 */
export interface ConfigRemoveFields {
  type: ConfigRemoveFieldsType;
  /** A list of field pointers to be removed from the config. */
  field_pointers: string[][];
  /** Fields will be removed if expression is evaluated to True. */
  condition?: string;
}

export type CustomConfigTransformationType =
  (typeof CustomConfigTransformationType)[keyof typeof CustomConfigTransformationType];

// eslint-disable-next-line @typescript-eslint/no-redeclare
export const CustomConfigTransformationType = {
  CustomConfigTransformation: "CustomConfigTransformation",
} as const;

/**
 * Additional parameters to be passed to the custom config transformation.
 */
export type CustomConfigTransformationParameters = { [key: string]: unknown };

/**
 * A custom config transformation that can be used to transform the connector configuration.
 */
export interface CustomConfigTransformation {
  type: CustomConfigTransformationType;
  /** Fully-qualified name of the class that will be implementing the custom config transformation. The format is `source_<name>.<package>.<class_name>`. */
  class_name: string;
  /** Additional parameters to be passed to the custom config transformation. */
  $parameters?: CustomConfigTransformationParameters;
}

export type ConnectorManifest = DeclarativeComponentSchema;
